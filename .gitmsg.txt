Electron 顶层菜单与右键菜单改为中文

- 新增 buildMenu()：文件 / 编辑 / 视图 / 窗口 / 帮助 全中文；macOS 增加应用名首菜单（关于、服务、隐藏、显示全部、退出）
- Electron 的 role 只负责行为不负责翻译，label 逐个显式写中文；app.name 提前设置供 macOS 菜单与「关于」使用
- 新增 attachContextMenu()：界面右键菜单中文化（剪切/复制/粘贴/全选/检查元素），按 editFlags 控制可用态
- 文件菜单接入实际操作：启动/停止服务、跑一轮决策、打开数据文件与 state / logs 目录，并配快捷键
- 修复：「跑一轮」原判断为 st?.dryRun，设置缺失时不会传 --dry-run（可能下真实单）；改为 !== false 默认演练，与启动服务一致
