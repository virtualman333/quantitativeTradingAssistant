# 2026-09-05

## UI 导航两级化 + 策略回测独立 tab（已完成并提交 0177321/3071111，已 push）
- 用户确认采用「两行式」导航并按板块分组：总览 / 交易(行情·持仓·超短线·策略回测) / 运行(对话·观测·日志) / 复盘(报告) / 系统(模型·角色·MCP·Skill·设置)。
- App.vue：原平铺 13 tabs 改为 `NAV` 板块结构；单叶板块（总览/复盘）点大菜单直达、多叶板块展开二级 `.tabs`；叶子 key（dash/mkt/scalp/bt…）全部保持稳定，hash 直达/KeepAlive/goTab 不受影响；`lastLeaf` 记住每板块最近停留叶子。一级菜单样式 `.menu/.menu-item` 加在 main.css。
- ScalperView 瘦身：策略库+回测拆走；顶部新增「实盘策略」tag + 「去策略库 · 回测」入口（goTab('bt')）。
- 新增 `ui/components/BacktestView.vue`：策略库（内置+自定义/LLM 生成/校验/应用到循环）+ 回测控制台/进度/结果/明细/编辑弹窗，全部从原 ScalperView 平移。
- 提交时顺带提交了此前遗留的 `strategies/趋势跟踪优化止盈止损/`（上轮「生成常用策略」产物）。

## 策略接口支持自定义止盈止损点位 sl/tp（提交 79689a1，push 网络 reset 失败，本地提交）
- 需求：策略能自定止盈止损点位，回测与实盘同步。实现为 `signal(ctx)` 返回值新增可选 `sl`/`tp`（绝对价，以 `ctx.price` 为参照）。
- `scripts/strategy_loader.py`：`call_signal` 解析 sl/tp（flat 置 None）；新增共用 `resolve_stops(direction, ref, sig, fallback_sl_dist, fallback_rr)` → (sl_dist, tp_dist, rr, used_direct, note)，校验「多单 sl<price<tp / 空单反之」+tp 更远，非法回退默认并给中文 note。
- `scalper.py`（实盘信号）与 `scalper_backtest.py`（回测）都改走 resolve_stops：实盘 ref=最新收盘 last，回测 ref=信号根收盘 c[i]、入场下根开盘重建——两端口径一致；src/scalper.ts 本就按 `sl_dist/tp_dist+entry_ref` 重建挂单，无需改逻辑，仅补 `stop_note?` 接口字段。
- `src/strategies.ts` generateSystemPrompt 补 sl/tp 字段与用法示例；strategy_check.py 冒烟校验 sl/tp 须数值。
- 教训：该轮 docstring 里含特殊 Unicode（∈ 等）的两行 replace 反复失败但代码块 replace 正常——遇到难匹配行直接放弃微调即可，别死磕。

## 策略编辑「按代码回填」元信息（提交 ad2d410，已 push：3071111..ad2d410 含此前 79689a1）
- 需求：已有 strategy.py（贴入/手写）时，让 LLM 反推 名称/描述/思路，三项可分别勾选覆盖。
- 后端 `src/strategies.ts`：meta.json 新增 `idea` 字段（持久化思路，saveStrategy 收 idea?，openEdit 回显）；新增 `backfillMetaFromCode({code, wantName, wantDesc, wantIdea, nameHint})` → {ok,name?,desc?,idea?,modelId?,error?}；system prompt 约束只输出 JSON，用 extractJsonish（围栏/首尾花括号）宽松解析。
- IPC 链路：`strategy:backfill` handler（electron/main.ts）→ preload `strategyBackfill` → api.js 自动透传（buildApi 枚举 window.api 方法）。
- UI BacktestView 编辑弹窗：代码框下方新增一行 checkbox（名称/描述/思路，默认全勾）+「LLM 按代码回填选中字段」按钮；回填只覆盖勾选项；保存带 idea。
- 经验：同文件多个 replace 并行易触发 30s 写锁超时，超时的替换单独重发即可；读小文件时 offset 超过文件行数会被误报 "File is empty"。

## 策略库内置常用策略 + 分类 + 显示优化（提交 45be11c，已 push）
- 内置 7 个：sample-trend 斜率顺势、双均线趋势跟随、MACD 趋势跟随（趋势跟踪）；布林带回归、RSI 超买超卖反转（均值回归）；唐奇安通道突破、放量突破（突破通道）。每个含 meta.builtin=true，文件已入库。
- 机制：`BUILTIN_STRATEGIES`（TS 内）为唯一事实源；`ensureBuiltins()` 比对磁盘 strategy.py/meta.json（strategy.py 用 trimEnd 比对，避免行尾换行差异导致每次 IPC 反复重写）不一致才还原；electron `loadStrategies()`（main.ts）每次加载前调用。内置不可删除（deleteStrategy 抛错）、不可覆盖（saveStrategy 遇 builtin 强制按名称 ensureIdExists 另存）。
- meta.json 新增 `category`（趋势跟踪/均值回归/突破通道/自定义）；readMeta 读侧兜底（旧数据内置→趋势跟踪、其余→自定义），列表 listStrategies 内置优先（按 BUILTIN 顺序）再按 updatedAt。
- UI BacktestView：分类 chips 筛选（filterCat + catCounts）+ 按分类 tbody v-for 分组（组标题行 cat-row）；策略名徽标 引擎内置(t-hold)/内置(t-info)/我的(t-buy)；引擎默认规则并入趋势跟踪组作 ENGINE_ENTRY(id:'')；内置行仅「复制为自定义」（openCopyStrategy → mode copy 名称自动加「副本」预填代码/元数据另存）；编辑弹窗新增分类下拉。
- 教训：Vue 表格里想给 tbody v-for 分组，直接 `<tbody v-for>` 即可；误用 `<template v-for>` 包 tbody 时把 `<td colspan>` 行误删过——同文件内连续 edit 别贪多，宁可多读一次。

## LLM 生成提示词去模板化（d31b46e，已 push）
- 根因：旧 `REFERENCE_TEMPLATE` 是「内置趋势 5 根斜率判向」完整实现贴 system prompt 底部，LLM 天然照抄 → 生成物全是 5 根斜率，与用户思路脱节。
- 修复（src/strategies.ts）：角色增强为「资深量化研究员、从精确复刻用户判断逻辑出发、禁止套经典套路」；完整参考模板换成 `INTERFACE_SKELETON`（只演示 ctx 读取/返回写法，刻意无任何方向判断算法）；新增「写码前自查」清单（用户思路每个条件必须落地、reason 写真实触发依据、阈值用相对量）；新建/改进两模式任务指令分别强化（新建=逐条落地不许偷换金叉/布林/RSI/斜率等未提及套路；改进=原代码基线做针对性增强，除非明确要求否则不推倒重写）。文件头注释同步更新。

## 策略批量回测 + 科技感 UI（9e0bd25 部分 + 1e8944b，已 push）
- 需求：策略表格支持勾选多选 →「批量回测选中」，及一键「回测全部」（含引擎内置基准行）。
- 前端 BacktestView.vue 全部自持：勾选 key（引擎行 key=`__engine`）、allRows/全选/反选；批量=对 btForm 同一套参数逐个串行 `scalperBtStart`+`scalperBtGet` 轮询兜底（waitBtJob 2.2s 轮询，后端 240s 自毙）；每完成一个 `noteRowRes` 把摘要写进该行，用 sessionStorage(`btBatchResV1`) 保留到关窗；批量队列可「停止后续」；单跑结果同样回填行内；`btBusy` 互斥单跑/批量。参数抽成 `btParams()` 保证对比公平。
- 表结构重排：新增首列 checkbox（th 全选）、删除独立「更新时间」列并入名称下小字、新增「最近回测」列展示 净盈亏%/笔数/胜率/PF/回撤 与失败标记。
- 美化（scoped，全部用全局 CSS 变量，浅深主题自适应）：策略库顶部渐变 hero（图标+文案+统计卡 总数/内置/我的/已回测）、批量操作条（虚线框）、行选中高亮（sel 行 inset 蓝边）、运行状态 bt-dot 光点脉冲、流动进度条 btFlow；注意 `color-mix()` 已换回 `var(--surface-2)` 兼容性。
- 技术细节：批量状态全部前端内存态，无需改后端 job 并发逻辑（每 job 独立子进程天然并发）；构建 `pnpm run build`（三套 tsc + vite）一次通过。

## 回测参数丰富化 + 自动保存（169630e，已 push）
- 引擎 scalper_backtest.py 新增：`--rr` 固定止盈 RR（0=自动，优先级：策略显式 rr > 参数 rr > 凯利）；`--slippage-bps` 单边滑点，开仓/平仓成交价都按不利方向计入（entry 用 o[i+1]±slip，平仓 fill=目标价±slip，sl/tp 仍按无滑点触发判定）；`--max-hold` 持仓超时（以当前收盘价平，reason=持仓超时，放在反转平仓之后）。params 输出带 rr/slippageBps/maxHold。
- src/scalper.ts backtestArgv 加透传（`(args.rr??0)>0` 才带参）；UI btParams() 统一供单跑与批量队列使用 → 对比公平。
- UI 控制台重排为三分区（标的与策略 / 时间区间 / 进出场与成本），新增字段：RR、滑点 bps、持仓超时、名义金额独立；标的 datalist 常用币；时间快捷 chips 近1天~近1月（quickRange 把 end=now）。摘要 buildBtSummary 带 ATR/RR/滑点/超时快照（取自 python params）。
- **参数自动保存**：localStorage `btParamsV1`（{v:1,f:{...}}），watch(btForm,deep) 350ms 防抖写入；btFormDefaults() 生成默认并兜底合并缺字段；「恢复默认参数」按钮 reset+立即写；loadStrategies 里保存的 strategyId 若已删则回退跟随实盘。
- 坑：对同一文件并行 replace_in_file 会拿文件锁互斥，多个中部分超时未写入且无报错 → 回测脚本 force_rr 分支一度没落盘（pyright 报 force_rr unused 才抓到）；单文件改动务必串行 + 改后 py_compile/read 抽查。
- 验证：6h 真回测冒烟 OK（fee 双边正确）；pnpm build 全绿。
