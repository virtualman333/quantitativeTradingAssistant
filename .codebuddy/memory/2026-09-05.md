# 2026-09-05

## 超短线回测：多 K 线周期 + 多时段（按天/周/月）批量
用户需求澄清：跑近 1 个月区间、按天分割，期望每天一份回测结果。

完成改动：
- Python `scripts/scalper_backtest.py`：数据层重构为「1m 唯一网络拉取 + SQLite 缓存 + 其余周期本地聚合」。
  - `bar_ms()` 周期→毫秒；`fetch_okx`/`fetch_history` 支持任意周期参数。
  - `_gaps/_row_hits/_agg_bucket`：覆盖判定（末端容差 1 根、根数差 ≤2）、缺口按段补拉、有洞回退整段、聚合写 bar 层缓存。
  - `run(..., bar="1m")`：max-hold 分钟→按周期根数折算（ceil）；进度/报错文案带周期名。
  - argparse `--bar`（choices 1m…1D，默认 1m），result/params 带 bar。
- `src/scalper.ts`：`backtestArgv` / `runScalperBacktest` 增加 `bar` 透传（≠1m 才加 `--bar`）。
- `electron/main.ts`：看门狗 240s → 600s（支持整段近 1 月首次拉取），UI wait 超时同步 616s。
- `ui/components/BacktestView.vue`：
  - 表单新增 `bars`（多选周期 chips：1m/5m/15m/30m/1H/4H）与 `split`（whole/day/week/month），自动持久化。
  - `winSegs()` 拆窗口、`expandRows()` 策略×周期×时段笛卡尔展开、`btDimText/batchConfirmMsg`。
  - `runBatch` 按格覆盖 bar/start/end 参数串行跑；展开态结果维护在「批量回测矩阵」面板（lastGrid + gridStateC，含逐格实时进度与摘要/失败信息），单格态沿用行内 noteRowRes。
  - runSelBatch/runAllBatch/runBacktest 主按钮都改为展开逻辑；主按钮文字随 expandDim 提示网格规模。

验证：`pnpm run build` 通过；5m 跑 6h 冒烟 OK（首次 fetched=608 根 1m、二跑 fetched=0 全命中缓存）。

## 批量回测多进程并行
用户需求：涉及批量回测支持多线程回测。
- Python `scalper_backtest.py` `_db()`：`isolation_level=None`（autocommit）+ `PRAGMA journal_mode=WAL` + `busy_timeout=30000`，新增 `_tx()` 用 `BEGIN IMMEDIATE`…COMMIT/ROLLBACK 包单批写入（`_write_1m`/`_agg_bucket` 改用它）→ 多进程并发写缓存不再有「先读后写锁升级死锁 / database is locked」。
- UI `BacktestView.vue`：
  - 表单新增 `concurrency`（默认 3，持久化；chips 1~4 路），`btConcurrency()` 解析并封顶。
  - `runBatch` 串行 for 循环 → worker 池（`Promise.all` N 个 worker 领号跑格，`live[key]` 天然 keyed 支持多格同跑）；行内/矩阵「运行中 xx%」本来就是按 key 独立显示，天然适配并行。
  - 批量进度条改为总体估算：完成数 + 各进行中格的实时 p 累加；bt-strip 头部显示「完成/总数 · N 路并行中」，下面逐条列出各进行中格名称+百分比。
  - 「停止后续」语义不变：正在跑的格跑完即止，不再领新格。
- main.ts 无需改（每个 job 独立 spawn/rec/Map/广播，本来就并发安全）。

验证：`pnpm run build` 通过；3 个进程同时冷区间回测（5m/15m/1H 重叠取数与聚合写库）全部 rc=0，无锁错误。

## 回测数据拉取进度 + 模块 UX 优化
用户需求：回测拉数据阶段要显示「数据拉取中」+ 进度；整个回测模块有点乱，优化体验。
- Python `scalper_backtest.py`：`fetch_history(..., job_id="")` 每补一段缺口上报 `report_progress(p=1+fetched//400 封顶4, stage="data", msg="数据拉取中：已入库 N 根 1m（ts 起）…")`；取数完成后 `run()` 报 `stage="backtest"` "数据就绪：缓存命中 X 根，本次新拉 Y 根，开始逐根回放…"。
- UI `BacktestView.vue`：
  - 新增 `stageLabel()`：data→「数据拉取中」、backtest→「逐根回放中」、启动→「启动中」；单跑进度、批量 bt-strip、矩阵运行中、策略行 live 四处统一用它，完整 msg 放 title。
  - 单跑进度条从 `.sg-pbar` 改为复用 `.bt-track/.bt-fill`（流动动画，与批量一致）。
  - 面板顺序调整：策略库 → 回测控制台 → 批量回测矩阵 → 单跑结果/明细（矩阵从策略库后移到控制台后，发起→结果更顺）。
  - 控制台分组合并：去掉「并行度」独立分组标题，并行路数并入「时间区间」分组（时段拆分下一行）；hero 文案精简。
验证：`pnpm run build` 通过；冷区间 5m 回测 stderr 进度序列正常（data 1→4→backtest 5 数据就绪→54 回放→100 完成）。

## 回测详情查看 + LLM 分析
用户需求：支持查看详细回测信息、支持回测信息 LLM 分析。
- 后端：
  - `src/scalper.ts` 新增 `analyzeBacktest(result, strategyName)`：用 `resolveModel()` + `createProvider(cfg, false)` + `llm.complete()`（非 decide，避免 extractJson）生成中文 Markdown 分析；mock/未配置返回友好错误；交易明细只取最近 60 笔关键字段防超长。系统提示要求：结论→指标解读→结合每笔问题→2~3 条可落地建议，≤400 字。
  - `electron/main.ts` 注册 `scalper:btAnalyze` IPC；`preload.ts` 暴露 `scalperBtAnalyze`。
- 前端 `BacktestView.vue`：
  - 新增内存缓存 `btResults[key]=完整 result`（sessionStorage 仍只存 summary 防膨胀）；批量 runCell 与单跑 noteRowRes 都写入；矩阵格 gridPatch 额外存 result。
  - 新增回测详情弹窗 `btDetail`：摘要卡片 + 「LLM 分析」按钮（`runAnalyze`，loading 态）+ Markdown 渲染（`renderMd`：先 escapeHtml 再套标题/列表/加粗/行内代码，v-html）+ 交易明细表。
  - 三个入口：单跑结果摘要行「查看详情 / AI 分析」按钮、批量矩阵每格「详情」按钮、策略库每行操作列「详情」按钮（btResults 命中时）。
验证：`pnpm run build` 通过；lint 无错（LLM 实际调用依赖真实模型配置，未做运行时验证）。


