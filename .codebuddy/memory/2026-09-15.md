# 2026-09-15

## git 仓库根迁移 + 首个 Release
- 需求：让 `agent/`（当前工作目录）成为 git 主目录，并发布 release。
- 做法：父仓库（原根 `OKX Trader\`，所有文件带 `agent/` 前缀）先提交待改 → 建 `backup/agent-prefix` 分支并推送（旧历史备份）→ `git subtree split --prefix=agent -b agent-root` 重写历史 → 在 `agent/` 里 `git init` + `fetch .. agent-root` + `reset --hard FETCH_HEAD` 接管历史 → 补 `.gitignore` → 强推 `main`。
- 远程几经改名，最终为 `https://github.com/virtualman333/quantitativeTradingAssistant.git`（`git remote set-url` 已同步）。push 时 GitHub 会提示 "repository moved"，以提示的新地址为准。
- Release：本机无 gh CLI，改用 `git credential fill < 输入文件`（写 `protocol=https\nhost=github.com\n\n`）从 Windows 凭据管理器取 PAT，再 `Invoke-RestMethod` POST `/repos/{owner}/{repo}/releases` 创建。tag `v0.1.0` 已推送，Release 地址 `/releases/tag/v0.1.0`。用完即删临时文件（含 token）。
- 注意：父目录 `OKX Trader\.git` 因被占用（Access denied）未能改名，仍在；不影响 agent 仓库，可手动删除（旧历史已在 `backup/agent-prefix` 分支）。
- 坑：本工具里 `cd /d ... ; powershell ...` 混写会让整行被 PowerShell 解析（`cd /d` 报错且 cwd 变成工作目录）→ 需要 PS 时整条命令都写成 `powershell -NoProfile -Command/-File`，并用绝对路径；不要在混写命令里做重命名（曾误把 agent/.git 改名）。

## README 更名 + Release 中文乱码修复
- README 标题改为 `quantitativeTradingAssistant（量化交易助手）`，删掉过时的「与父目录的关系」章节（仓库根已是工程本体），目录树前缀 `agent/` → `quantitativeTradingAssistant/`，补 `strategies/`、`skills/` 并修正 `scripts/` 描述。
- **Release 中文乱码根因**：用 PowerShell 生成 Release 时，脚本文件是无 BOM 的 UTF-8，Windows PowerShell 5.1 按 ANSI(GBK) 解析脚本 → 中文字符串在解析阶段就已损坏。修复：改用 Python 脚本（源文件默认 UTF-8）组 JSON + `ensure_ascii=False` 后以 UTF-8 bytes POST。
- **GitHub API 坑**：`/repos/{owner}/{repo}/releases/tags/{tag}` 只支持 GET（PATCH 返回 404，会误判成「不存在」）；更新 Release 必须先 GET 拿 `id` 再 `PATCH /releases/{id}`（POST 重复创建返回 422 already_exists）。
- 发布/更新 Release 的完整套路（本机无 gh CLI）：`git credential fill < 含 protocol=https/host=github.com 的输入文件 > 输出文件` 取 `password=` 即 PAT → Python urllib 调 API → 用完即删临时文件。

## 远程分支清理
- 按用户要求删除远程 `backup/agent-prefix`（旧 `agent/` 前缀结构备份），远程现只有 `main` + tag `v0.1.0`。
- 迁移前的完整旧历史目前**只存在于本机父目录** `OKX Trader\.git`（因被占用未能删除/改名），如需回滚旧结构可从那里取。
- 本地与远程核对方法：`git rev-list --left-right --count HEAD...origin/main`（0 0 即同步）+ `git diff HEAD origin/main`（空即无差异）；tag 落后 main 属正常（`v0.1.0` 指向 9c40087，main 已到 cc426a5）。
