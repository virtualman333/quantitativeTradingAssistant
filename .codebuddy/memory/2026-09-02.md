# 2026-09-02

- 设置 .gitignore：仓库根位于 `E:/ai_project/okx_trader_agent/`（git rev-parse 确认，注意 agent/ 只是子目录）。在根 .gitignore 中为 agent TypeScript/Electron 子项目新增规则：`node_modules/`、`agent/dist/`、`agent/release/`、`*.tsbuildinfo`。提交 a0c35d6。push 因网络连接重置失败（github.com 无法访问），需稍后重试。
- 现状：agent 下 electron/、ui/、src/experts.ts、src/graph.ts、src/mcp.ts、src/orchestrator.ts、src/skills.ts、pnpm-lock.yaml、pnpm-workspace.yaml 等仍为未跟踪新文件；package.json、src/main.ts、src/okx.ts、tsconfig.json 有未提交修改，均属于先前遗留工作。
- 全面通读父目录+agent：项目 = 父目录 Python 交易体系（章程 v2.0 L1/L2 分层，scripts/*.py，运行时数据 logs/state/ledger 等）+ agent TS LangGraph 多专家常驻 Agent（store.ts 持久化、okx.ts 复跑 py 脚本、electron UI）。已整理长期要点至 MEMORY.md。
