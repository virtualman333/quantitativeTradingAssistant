# 2026-09-03 工作记录

## Electron 顶层菜单中文化（agent/electron/main.ts）
- 原状：未设置菜单，走 Electron 默认英文菜单（File / Edit / View / Window / Help），右键菜单也是英文。
- 新增 `buildMenu()`：文件 / 编辑 / 视图 / 窗口 / 帮助 五组全中文；macOS 额外加首项「应用名」菜单（关于 / 服务 / 隐藏 / 隐藏其他 / 显示全部 / 退出）。
- 关键点：**`role` 只负责行为，不负责翻译**，label 必须逐个写中文，否则仍显示英文。
- `app.name = "OKX 交易 Agent"` 必须在 app ready 前设置，macOS 菜单首项与「关于」才会用到它。
- 新增 `attachContextMenu(win)`：右键菜单的中文化（剪切/复制/粘贴/全选/检查元素），用 `params.editFlags` 控制可用状态。
- 文件菜单里接了实际动作：启动服务(CmdOrCtrl+Shift+S)、停止服务(Shift+X)、跑一轮决策(Shift+O)、打开数据文件(Shift+D)、打开 state / logs 目录。

## 顺带修掉的隐患
- `runOnceAgent()`（原 `agent:runOnce` 内联逻辑）原先判断 `st?.dryRun ? ["--dry-run"] : []`：设置缺失时会**不带演练标志直接跑一轮**（可能下真实单）。改为 `!== false`（默认演练），与 `startAgent()` 行为一致。该函数已抽出，界面按钮与顶层菜单共用。

## 修复「模型调用失败(Hy3-CodeBuddy)」HTTP 400 / 11101（src/llm.ts）
- 现象：专家全部 abstain，界面提示 `模型调用失败(Hy3-CodeBuddy): ... code 11101 Non-stream chat request is currently not supported`。报错文案出自 `graph.ts:100`（decide 异常被吞成 abstain）。
- 根因：`OpenAICompatProvider.decide()` 发的是**非流式**请求；而 `copilot.tencent.com/v2` 网关只支持流式。对话页走 `streamChat`（流式）因此正常 —— 这就是「对话能用、跑一轮/测试连接失败」的原因。
- 修复：decide() 遇到 400 且响应体匹配 `non-?stream|11101|only stream|只支持流式` 时，把模型 id 记入模块级 `streamOnlyModels` Set，改走 `openaiChat` 流式拼接结果，调用方（专家/调度/主 Agent/testModel）无感知；非该类错误（鉴权、参数）仍原样抛出，不掩盖真实问题。
- 未做验证：联网实测未跑成（tsx --eval 相对路径解析失败，且用户取消了命令）；`tsc -p tsconfig.json --noEmit` 已通过。建议用界面「测试连接」确认。
- 附：当前默认模型是 store.json（已 gitignore）里的 `Hy3-CodeBuddy`（`https://copilot.tencent.com/v2`，model `hy3`），Key 也在该文件里，勿外泄。

## 修正：传回 max_tokens 并将默认阈值提到 16000（src/llm.ts, commit c03fc59）
- 用户意图反复：先说"不需要 max_tokens 这个" → 后又改口"还是传 max-token 吧""但是要提高阈值"。最终方案：OpenAI 兼容 / Anthropic / Claude 原生三路径**都传 max_tokens**，默认预算从 8000 提到 **16000**。
- 恢复 `DEFAULT_MAX_TOKENS = 16000`，Anthropic 原生(原 2000)、Claude 原生(原 3000) 默认值统一引用该常量；推理模型正文被预算截断(finish=length)时 decide() 翻倍(16000→32000)再试一次兜底。
- 已确认 store.json 未给模型单独配 maxTokens，默认 16000 生效；已删除临时排查脚本 scripts/tmp-llm-diag.ts；tsc 通过并已提交。

## 修复：设置/模型等点击保存报 "An object could not be cloned"（electron/preload.ts, commit 4066a58）
- 现象：设置页（ModelsView/RolesView/McpView 同理）点保存抛 `保存失败：An object could not be cloned`。
- 根因：Vue `ref`/`reactive` 表单对象是 Proxy；Electron 的 `ipcRenderer.invoke` 用 `structuredClone` 序列化参数，Proxy 触发 DataCloneError。传参里含 `form.value.fixedRoles`（reactive 数组）等就会炸。
- 修复：preload 新增 `safeInvoke`，对所有 `invoke` 参数先做 `JSON.parse(JSON.stringify(a))` 拍成纯对象（所有数据本就可 JSON 化），渲染进程所有 `api.*` 调用统一走它，全局覆盖设置/模型/角色/MCP 各页。
- 踩坑：最初 `replace_all` 把 `safeInvoke` 函数体内那行 `ipcRenderer.invoke` 也一并改成 `safeInvoke` 造成无限递归，已改回。tsc 不报该运行时错，肉眼核过第 10 行。

## 新增「持仓」页签：实时查看 OKX 账户仓位（commit 44bad67）
- 背景：Dashboard 的"持仓"面板数据来自 `status.latestRound.positions`（最近一轮决策快照文件 state/round_input_R*.json），只有 agent 跑轮才更新，且不跑轮就显示"暂无持仓"——不是实时查看。用户要"支持仓位信息查看"。
- 实现：新增独立「持仓」页签，`PositionsView.vue` 调 `api.accountGet(profile)` 实时拉取。
  - 主进程 `electron/main.ts` 新增 `account:get` handler，动态 `import("file://"+AGENT_ROOT/dist/src/okx.js)` 调 `fetchAccount(profile)`（读操作）。
  - `src/okx.ts` 的 `fetchAccount` 增加 `profile: "demo"|"live" = "demo"` 参数，内部三次 `mcpCall` 改用该 profile；live 为只读监控，符合 L1-3。
  - `preload.ts` 暴露 `accountGet(profile?)`（走 safeInvoke，参数自动 JSON 拍平）。
  - `App.vue` 注册页签（列表图标）+ `main.css` 补 `.head-row` 弹性行样式（沿用 :root 变量）。
- 展示：权益/可用/浮盈卡片 + 持仓表(标的/方向/张数/开仓均价/标记价/杠杆/浮盈/浮盈%/保证金/强平价) + 挂单表(标的/类型/方向/数量/止损/止盈/状态) + demo/live 切换 + 手动刷新。字段做了容错映射（OKX 原始字段 pos 带符号、avgPx/markPx/upl/margin/liqPx 等）。
- tsc 三套（tsconfig.json / electron / preload）均通过。
- PowerShell 命令行中文双/单引号均报"字符串缺少终止符"，改用 `git commit -F commit_msg.tmp` 文件方式提交成功（临时文件已删）。

## 架构转向：持仓查看改为「LLM 调 MCP 多交易所」（取代 account:get 的 OKX 硬编码）
- 用户明确：未来会接多个交易所，API 各异，持仓查看这类"读不同所数据+归并展示"不该在 UI/代码里为每个所写死，而应**交给 LLM 通过 MCP 调各交易所 server**，归并成统一 schema；展示要"两者都要"——结构化表格(统一 schema) + LLM 文字解读/风险。适配层=各交易所自己的 MCP server（用户在 MCP 页添加），agent 不写 per-exchange 代码。
- 落地（本次）：
  - `src/types.ts` 新增统一 schema：`UnifiedAccount / UnifiedPosition / UnifiedOrder / PortfolioSnapshot / PortfolioSummary`（交易所无关，LLM 负责归并）。
  - `src/tools/mcpBridge.ts`（新）：`loadMcpReadTools()` 连所有 enabled MCP server，按名/描述启发式**只桥接只读工具**（`isReadOnlyMcpTool`：排除 place/cancel/set/transfer 等写动词，白名单 get/query/list/balance/position/order/...），写工具一律走 okx.ts 受控通道，守 L1-3。返回 close 由调用方释放连接。
  - `src/portfolio.ts`（新）：`summarizePortfolio()` 自带精简 ReAct 循环（复用 `llm.streamChat`），让 LLM 调只读 MCP 工具 → 输出统一 schema JSON + 中文 notes，过程流式事件回传（exchanges/delta/tool_start/tool_result/done/error）。**与对话页 chat.ts 隔离**，避免把 MCP 工具混入全局对话工具集（防写工具意外暴露）。
  - `electron/main.ts` 新增 `portfolio:summarize`（流式 `portfolio:event`）+ `portfolio:abort`；`preload.ts` 暴露 `portfolioSummarize/portfolioAbort/onPortfolioEvent`。
  - `ui/components/PositionsView.vue` 重写：去掉 demo/live 切换与 OKX 字段硬编码，改为「汇总持仓」按钮 → 展示已连接交易所 chips、工具拉取过程、文字解读面板、账户卡片、统一持仓表(含交易所列)、挂单表。
  - `account:get` handler 保留（OKX demo/live 只读兜底），但持仓页签已不再使用它。
- 关键约束（长期）：MCP 暴露给 LLM 的只能是只读工具；多交易所扩展 = 加 MCP server，不动 UI/汇总引擎。

## 修复：点击「汇总持仓」报 api.portfolioSummarize is not a function（commit 6b04c38）
- 现象：界面点汇总持仓报 `api.portfolioSummarize is not a function`；根源不是 preload 缺函数（源码 electron/preload.ts 早就有），而是 **dist 运行产物过期**：`dist/preload/preload.js`、`dist/electron/main.js`、`dist/src/portfolio.js` 全都没有相关代码。
- 为什么产物过期：`src/tools/mcpBridge.ts` 里 import `"./mcp.js"` 写错（mcp.ts 在 src/ 根，应在 `../mcp.js`），导致 `npm run build` 的 `tsc -p tsconfig.json` 一直失败、dist 从未成功更新（tsx dev 能容忍该路径写法，所以平时没暴露）。修正 import 后完整 build 成功。
- 坑：Windows PowerShell 下 `npm run build` 里 tsc 报错时 npm 仍返回 exitCode 0（实际链路在第一步就断），判断失败要看 stdout 里有没有 postbuild/vite 输出。
- 顺带加固：`scripts/dev-ui.mjs` 现在启动前先 `compileOnce()`（三套 tsc + postbuild），避免改过 src/electron .ts 后 ui:dev 仍加载旧 preload 的静默失效。

## 新增：全局 LLM 调用观测（拒绝黑盒，commit 9cead64）
- 用户诉求：LLM 调用不能黑盒，要在对话里或"一个地方"能看到每次记录。
- 落地：主进程新增 `llm:trace` 广播总线。`electron/main.ts` 的 `wrapTrace(source, send)` 把 chat/portfolio 的业务事件**同时**转发到该总线（原 send 不变，页签照常渲染）；思考链(reasoning)按 source 累积、在下个轮次/工具调用/结束时 flush 成一条记录。
- `chat.ts`/`portfolio.ts` 之前**完全丢弃**了 `llm.streamChat` 产出的 `reasoning` 与"第几轮 LLM 调用"信息，本次补回：每轮开始发 `round`（模型 id + 消息数），流式转发 `reasoning` 思考链事件。
- 新增 `ui/components/TraceView.vue`「观测」页签（App.vue 加 tab `obs`）：订阅 `api.onLlmTrace`，按时间展示 轮次/思考过程/工具调用(args)/工具结果(output 截断 2000 字)/确认/结束/错误，支持 对话/持仓 过滤、清空、自动滚底；preload 暴露 `onLlmTrace`。
- 对话页内联展示：ChatView 把 reasoning 渲染成可折叠「思考过程」块（流式 + 历史消息都带）。
- 关键点：观测是"旁路广播"，不改动既有对话/持仓渲染路径；新增 LLM 调用源只需在 main 的 handler 里用 `wrapTrace` 包一层 onEvent。
