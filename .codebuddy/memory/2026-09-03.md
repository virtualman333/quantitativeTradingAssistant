# 2026-09-03

## 持仓汇总报错排查与修复
- 用户报「点击持仓汇总报错」，确认报错为 `api.portfolioSummarize is not a function`。
- 根因：`dist/electron/preload.js` 是早期配置遗留旧产物（tsconfig.electron.json 现只编 main.ts），缺新 API；`resolvePreload()` 原来优先选同目录文件 → 加载到旧 preload。
- 修复（1f738c8，用户同期有 0f4a71c）：resolvePreload 优先选 `dist/preload/preload.js`；postbuild.mjs 自动清理遗留 preload；已删现存遗留文件。
- 另修（d04fd07）：PositionsView.summarize() 返回 {ok:false} 时未复位 streaming，按钮卡死无法重试。
- 端到端验证：node 直跑 summarizePortfolio 全链路 OK（MCP 154 只读工具 + 真实模型出 schema）。
- push 多次失败（github 连不上），本地 0f4a71c/1f738c8/d04fd07 待推送。
