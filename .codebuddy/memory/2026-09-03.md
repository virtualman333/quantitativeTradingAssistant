# 2026-09-03

## 观测页接入 agent 轮次 LLM 轨迹（修复「没显示记录/没有实时」）
- 根因：观测页（TraceView）只接了主进程 chat/portfolio 两条路径，agent 子进程（跑一轮/常驻）的调度/专家/拍板 LLM 调用完全没接入。
- 方案：新建 `src/trace.ts`（AGENT_UI=1 时向 stdout 写 `__TRACE__` 前缀 JSON 行）→ `electron/main.ts` 的 `pipeAgentStdout()` 行缓冲解析（防 JSON 行被 chunk 劈半）→ 广播 `llm:trace`；TraceView 新增「轮次」来源与 round label 展示。
- 埋点：graph.ts 调度/拍板 + 模型失败错误事件；experts.ts 每次 LLM 调用（label 带「专家名·第 N 次调用」）+ skill/MCP 工具调用与结果。
- 已本地提交 5759ec0；push 网络失败（Connection reset），待重试。
