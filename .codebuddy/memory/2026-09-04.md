# 2026-09-04

## 超短线独立窗口进首页修复（90e8b11，已推送）
- 页签 key 是 `scalp`（不是 scalper），新窗口 hash 写成 #scalper 匹配不上 tabs → 首页。hash 已统一 `#scalp`。

## 超短线回测：独立窗口 + K 线 SQLite 缓存（90d8683，已推送）
- 缓存库 `agent/data/scalper_candles.db`（sqlite3 标准库），表 candles(inst,bar,ts,o,h,l,c,vol, PK(inst,bar,ts))；scalper_backtest.py 的 fetch_history 先查库，覆盖完整（末端容差 1 根 + 缺根容差 2）直接用，缺口才拉 OKX 并入库，结果带 cache{fromDb,fetched}。
- 独立窗口：scalper:openWindow 开新 BrowserWindow，同一 dist/ui 用 `#scalper` hash 直达（App.vue onMounted 解析 location.hash 设 tab）；窗口标题锁「超短线回测」供重开时 focus。
- 实测注意：本机直连 www.okx.com 全部超时（DNS 污染/断连，脚本有备用域名 aws.okx.com/okx.com 也会依次失败）；库命中时不走网络可正常回测。
- PowerShell 传含空格参数给 python 的坑：用 `--start '"2026-09-04 10:00:00"'`（外单引号内双引号），且时间必须带秒（脚本 strptime 格式 %H:%M:%S）。

## 策略库功能：多策略 + LLM 生成 + 回测进度 job（未提交 → 见提交 hash）
- 存储：`agent/strategies/<id>/{strategy.py, meta.json}`；含示例 `sample-trend`（等价内置趋势）。
- 策略接口（内置规则）：文件只许定义 signal(ctx) → dict{direction:long|short|flat, reason, atr_mult?, rr?}；ctx=closes/highs/lows/vols/ts/n/atr/price（已收盘）。由 scripts/strategy_loader.py（load_strategy+call_signal+make_ctx，异常兜底 flat）+ strategy_check.py（py_compile/红线 import 扫描/冒烟样例，白名单仅纯计算库）把关。
- scalper.py 与 scalper_backtest.py 都加 `--strategy <dir>`，未指定=内置逻辑（行为不变）；flat 支持观望；策略可覆盖 atr_mult/rr（引擎夹区间）。
- 回测 job：scalper_backtest.py `--job-id` 向 stderr 周期写 {"p","stage","msg"}；electron main.ts 的 scalper:btStart spawn python（watchdog 240s），进度经 `scalper:btEvent` 广播所有窗口，UI 实时进度条；scalper:btGet 兜底轮询。
- TS：src/strategies.ts（list/read/save/delete/apply/validate/generateStrategy）；store ScalperConfig 增 strategyId；scalper.ts fetchSignal/flat/runScalperBacktest/backtestArgv 接入；LLM 生成用 `llm.complete()`（不能用 decide——会 extractJson 截断 python 代码），system prompt 注入接口规范+安全红线+参考模板，输出 ```python 围栏提取。
- UI：ScalperView.vue 新增「策略库」面板（内置默认 + 自定义行：回测/应用到循环/编辑/删除）+ 新建/编辑 modal（名称/描述/思路→LLM 生成→代码手改→保存并校验）+ 回测面板加策略下拉与进度条；modal 复用全局 .modal/.box/.body/.foot。
- 实测：短回测 sample-trend 全链路 OK（进度 1→100，fetch 1112 根入库，47 笔交易）；本机无外网时回测走缓存可用。
- .gitignore 新增 data/scalper_candles.db、__pycache__/、*.pyc。
