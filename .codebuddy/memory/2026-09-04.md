# 2026-09-04

## 热门行情点击进新窗口显示首页 bug 修复
- 现象：点击「热门行情」交易对，新开窗口显示的是首页而非 K 线图。
- 根因：`electron/main.ts` 的 `loadUi()` dev 分支原来 `w.loadURL(devUrl + (hash ? "#" + hash : ""))`，而 `nav.js` 传的 hash 已带 `#`（形如 `#/win/kline?instId=xxx`），于是拼成 `##/win/kline?...` 双 `#` → `location.hash` 解析失败 → `parseRoute` 返回 null → `winRoute=null` → 独立窗口退化成渲染主界面（首页）。
- 修复：`loadUi` 统一先 `hash.replace(/^#/,"")` 去掉 #，各加载方式再自行加 #（dev 用 `devUrl + "#" + h`，dist 用 `loadFile(..., {hash: h})`）。dist 分支原逻辑本就正确（`hash.replace(/^#/,"")`），本次统一后两种模式都 robust。
- 复用窗口分支（`executeJavaScript("location.hash = ...")`）本就正确，未动。
- 独立窗口路由链路：nav.js `openKlineWin` → `#/win/kline?instId=...` → 主进程 `win:open` → `loadUi(w, hash)` → App.vue 按 `winRoute` 渲染 WinFrame。关键：hash 全程保持「带 # 一次」，不能在 loadURL 时二次加 #。

## 切换到对话页未滚动到底部 bug 修复
- 现象：切到「对话」页签，对话列表没自动滚到底部（最新消息处）。
- 根因：App.vue 用 `<KeepAlive>` 缓存视图，ChatView 只在 `onMounted` 里 `loadHistory()→scroll()`；切走再切回时组件已缓存，只触发 `onActivated`、不触发 `onMounted`，滚动位置停在切走前的位置。
- 修复：ChatView 补 `onActivated(() => scroll())`，KeepAlive 激活时重新滚到底部。scroll() 内部已用 nextTick，无需额外处理。
- 提醒：KeepAlive 缓存的视图，凡有「切回要刷新/复位」的逻辑（滚动、重拉数据），都要在 onActivated 里再做一次，不能只依赖 onMounted。

## 三项需求：观测 SQLite 持久化 + 轮次英文 + 报告中文
- 需求：①观测持久化到 SQLite ②轮次提示词英文优先 ③报告/汇总/总结类用中文（中文用户，但 LLM 内部用英文）。
- ①观测持久化：`electron/main.ts` 用 Node 内置 `node:sqlite`（`DatabaseSync`，Node 22.22 可用，免 better-sqlite3 的 Windows native 编译风险）。`emitTrace` 广播的同时 `persistObservation` 追加写 `data/observations.db` 的 `observations` 表（id/ts/source/kind/payload/created_at）。新增 IPC `obs:history`（倒序读最近 N 条）/`obs:clear`；preload 暴露 `obsHistory`/`obsClear`；`TraceView.vue` onMounted 先加载历史再收实时事件，「清空」同步清 SQLite。
- ②轮次英文：`llm.ts` 语言约束拆分 `LANG_HINT_EN`（"Reason and respond in English"）/ `LANG_HINT_ZH`。`decide()`（专家/调度/拍板）→ EN；`complete()`（报告）→ ZH；`streamChat()`（对话）→ ZH。
- ③报告中文：`report.ts` SYS 加「所有面向用户文字一律简体中文，数据标识符保持原样」。
- 注意：node:sqlite 是 experimental（会打 ExperimentalWarning，不影响功能）；obsDb 初始化失败时降级为仅内存广播，不阻断观测。

## K 线窗口：默认自动刷新 + 多种间隔
- 需求：默认自动刷新，增加 1 分 / 5 分等多种间隔。
- `KlineWindow.vue`：`auto` 默认 false→true；固定 `RELOAD_MS=20s` 改为 `INTERVALS` 选项（15 秒 / 30 秒 / 1 分 / 5 分），默认 30 秒；新增 `startAuto()`/`changeInterval()`，onMounted 里 `load() + startAuto()` 默认启动自动刷新；模板加间隔下拉框，切换间隔时若自动刷新开着就重设定时器。

## 纠正：间隔指 K 线图周期（bar），不是自动刷新时间
- 用户纠正：「间隔」指 K 线图的周期（bar 间隔），不是自动刷新时间；自动刷新应是秒级/ms 级。
- 修正：`KlineWindow.vue` 与 `MarketView.vue` 的 `BARS` 由 `["15m","1H","4H","1D"]` 增为 `["1m","5m","15m","1H","4H","1D"]`；自动刷新回退为固定秒级 `RELOAD_MS=10_000`（默认开启）；删掉误加的「刷新间隔下拉」（15秒/30秒/1分/5分）。
- 后端 `market:kline` 支持的 bar：`1m/3m/5m/15m/30m/1H/2H/4H/6H/12H/1D/1W`（electron/main.ts），故 1m/5m 可用。
- 教训：涉及「周期/间隔」的需求先分清是「图的数据粒度(bar)」还是「轮询频率(poll)」，两者完全不同。

## K 线自动刷新改为 1 秒（+ 防重入）
- 需求：自动刷新改为 1 秒；若 1 秒 REST 轮询不行，再上 SSE/WebSocket 连交易所。
- 改动：`KlineWindow.vue` 的 `RELOAD_MS` 10s→1s；`load()` 加防重入（busy 时跳过本轮，避免 1 秒轮询请求重叠）。
- 评估：OKX 公共行情 REST 限流约 20 次/2 秒（IP 级），1 秒 1 次 = 2 次/2 秒，远低于限流，不会触发。但 REST 轮询有往返延迟且每次拉 220 根，WebSocket（OKX `candle1m/5m/...` 频道实时推送）才是 ms 级真实时方案，作为后续备选。

## 修复 observations.db 被误提交到 git
- 观测 SQLite 持久化后，`data/observations.db` 被 `git add -A` 误提交并推送（含 LLM 调用轨迹，不应入库）。
- 修复：`agent/.gitignore` 加 `data/observations.db`；`git rm --cached` 移除（本地保留）。当时库内仅 1 条 round 记录，无敏感内容，风险低。
- 提醒：新增任何「运行时生成的数据库/日志」文件，务必同步加进 .gitignore，避免 `git add -A` 误提交。

## 告警邮件去重改为 24 小时持久化
- 需求：发送警告邮件不需要每次运行都发送。
- 原实现：内存 `Map` 30 分钟去重（重启失效，且 30 分钟对持续告警仍偏频繁）。
- 改为：告警记录（`state/alerts.jsonl`）每轮都追加（审计完整、与发邮件解耦）；邮件去重用 `state/mail_sent.json` 持久化，同 subject 24 小时内只发一次（重启仍有效）。邮件发送失败不记 sent，下次重试。

## 轮次提示词正文英文化 + 回复改中文（纠正上一轮「英文回复」）
- 用户纠正：之前「轮次英文」只改了语言指令 `LANG_HINT_EN`（要求英文回复），但**轮次提示词正文（sharedContext 及行情摘要）仍是中文**。要求：提示词正文改英文，但**回复要中文**。
- 改动三处：
  1. `src/main.ts` `sharedContext` 模板 → 英文：`Round Rxxxxxx, time ..., environment demo (paper trading)` / `[Account] equity ... USDT, available ...` / `[Positions] ...` / `[Algo Orders] ...` / `[Run State] day stop-loss ..., day PnL ..., month drawdown ...` / `[Candidate instruments & market digest] ...`；`buildMarketDigest` 的 catch 回退 `"行情不可用"` → `"market data unavailable"`。
  2. `src/llm.ts` `LANG_HINT_EN` → `"The prompt is in English; always respond in Simplified Chinese (简体中文) only."`（输入英文、输出中文）。decide() 路径（专家/调度/拍板）统一走它。
  3. `scripts/market_scan.py` `digest()` 输出英文化：`价→px / 共振→score / 量比→volR / 分位→range / 费→fund / 面值→ctVal / 步长→lotSz / 最小→minSz / 价格步→tickSz / 错误→err`；`已收盘K线不足`→`insufficient closed candles`、`扫描失败`→`scan failed`（这些 error 会经 str(exc) 进入 digest/instruments，随 sharedContext 进 LLM）。
- 边界：**system prompt（graph.ts 调度/拍板指令、experts.ts/experts/*.json 专家职责）仍是中文**，本次未动——它们属「固定指令层」而非「随轮次变化的数据输入」；且 mockReply/MockProvider 用中文关键词（`调度模块`/`主 Agent`）判断节点类型，英文化需连带改这些判断。若用户后续要一并英文化再单独做。
- 交叉引用注意：`【候选标的与行情摘要】` 这个标签在 system prompt（graph.ts 主 Agent 拍板、experts.ts trading/factor、expert.json）里被引用；本次 sharedContext 里改成了英文 `[Candidate instruments & market digest]`，与中文 system prompt 存在轻微措辞不一致（模型可语义理解），暂未同步改 system prompt。

## 轮次提示词增加「角色认知 + 目标 + 硬边界」背景段
- 需求：优化轮次提示词，增加角色认知、目标等信息。
- 改动：`src/main.ts` 新增模块级常量 `ROLE_MISSION`（英文，放在 sharedContext 最前），内容分三段：`[Role & Mission]`（你是 autonomous crypto perpetual-futures trading agent，多专家决策环；唯一目标=长期稳定盈利，月进阶 ≥+10%，"No trade" 合法但需理由）＋ `[Hard constraints — non-negotiable]`（USDT 永续、杠杆≤5、单笔风险≤2.5%、每仓必挂止损、禁亏损加仓、禁双向、月度回撤≥12% 停开新仓、live 只读）。对齐章程 §0（唯一目标）与 §1（L1 硬约束）。
- 设计考量：每轮重复注入（各 LLM 调用无跨轮记忆）；作为「全局背景」注入 sharedContext，不替代各 system prompt 的职责定义；用「You are part of ... trading agent」而非「You are the final decision-maker」，避免与专家/调度模块各自 system prompt 的角色冲突。

## 全部提示词英文化（系统扫描 + 批量替换）
- 需求：扫描全部提示词，都换成英文。
- 范围界定：**提示词 = 发给 LLM 的 system prompt / user prompt 模板 / 工具&skill 描述 / 输出契约 / 保密 preamble**。不改：日志、alert 邮件、UI 文案、代码注释、guard.ts 运行时 violations、运行时错误消息、mock 输出、LANG_HINT（语言指令，故意保留「简体中文」以要求中文回复）。
- 改动清单（约 33 文件）：
  1. `graph.ts`：planNode 调度 sys、adjudgeNode 拍板 sys + user（[Expert opinions]/confidence）、mockReply 关键词 `调度模块→dispatcher`、`主 Agent→Main Agent`。
  2. `experts.ts`：OUTPUT_CONTRACT、invoke 的 skillList/mcpList/sys 标签（[Skills you can call] 等）、user 标签、[Tool X returned]、reflectExperts sys/user、4 个 EXPERTS 兜底 base 的 name/duty/systemPrompt。
  3. `obfuscate.ts`：OBF_PREAMBLE → 英文保密协议。
  4. `llm.ts`：MockProvider 关键词判断 `调度模块→dispatcher`、`主 Agent→Main Agent`（mock 中文回复保留）。
  5. `report.ts`：SYS 指令英文化（保留 [Language] 输出中文要求）+ user prompt；确定性 HTML 兜底（中文）不改。
  6. `portfolio.ts`：SCHEMA_SPEC + SYSTEM_PROMPT 英文化（保留 notes 中文解读）+ user prompt。
  7. `chat.ts`：systemPrompt 英文化（保留「回答用中文」）。
  8. `tools/fs.ts|web.ts|project.ts|bash.ts`：全部工具 description + 参数 description 英文化（run 里中文输出保留）。
  9. `experts/*.json`（8 个）：name/duty/systemPrompt 英文化。
  10. `skills/*.json`（15 个）：name/description/args 英文化。
  11. `orchestrator.ts`（死代码，未被 import，已被 graph.ts 取代）：同步英文化防未来误用。
- 关键一致性：①交叉引用「候选标的与行情摘要」统一改为 `[Candidate instruments & market digest]`（sharedContext/拍板 sys/trading/factor prompt 一致）；②mock 关键词判断（graph.ts mockReply 与 llm.ts MockProvider）必须与 sys 里的英文词一致，否则 mock 模式节点类型判断失效。
- 验证：全部 TS lint 0 错误；experts/ + skills/ 全部 json `JSON.parse` 通过、无中文字符。
- 备注：name/duty 英文化后，UI「角色页」与「Skill 页」会显示英文名称（副作用，符合「全部英文化」）。

## 重点关注标的功能（每轮优先关注选中标的）
- 需求：支持「重点关注」——选中后每轮优先关注选中的重点。判定为**重点关注标的（instruments）**（交易系统核心=选标的；「专家」已有 alwaysInvoke/fixedRoles 机制）。
- 数据流：`store.settings.focusInsts: string[]`（新增字段，defaults `[]`）→ 主流程 `getSettings().focusInsts` → `fetchMarket(focusInsts)` → `market_scan.py --focus <逗号分隔>` → 候选池「重点标的优先 + top N 去重补齐」→ sharedContext 里注入 `[Focus instruments — prioritize these]` 提示。
- 改动 5 文件：
  1. `store.ts`：AppSettings 加 `focusInsts: string[]`，defaults 加 `focusInsts: []`。
  2. `okx.ts`：`fetchMarket(focusInsts)`，有则拼 `--focus` 参数。
  3. `main.ts`：runRound 读 `getSettings().focusInsts`（filter 去空），传 fetchMarket；sharedContext 候选标的行追加 `focusLine`（有重点则注入 `[Focus instruments — prioritize these] ... Analyze and act on them first; skip only with a clear reason.`）。
  4. `market_scan.py`：`--focus` 参数；候选池逻辑 `if focus: 去重后 focus + top_insts 合并（重点标的即使不在 top N 也纳入并排最前）`。
  5. `SettingsView.vue`：设置页「运行参数」加「重点关注标的」输入框（`focusStr` computed：数组 ⇄ 逗号分隔字符串，支持中英文逗号/空格分隔），save 时写 `focusInsts`。
- 效果：选中标的即使不在成交额 top 15，也会被 market_scan 扫描、纳入候选池并排在 digest 最前；AI 在 prompt 里看到明确「优先关注」指令。旧 store 升级后 focusInsts 自动 `[]`（loadStore 的 settings spread 兜底）。
- 验证：TS lint 0 错误、market_scan.py py_compile 通过。

## 超短线（超高频）独立板块
- 需求：新增独立功能板块（与主轮次解耦），超短线：凯利公式算止盈止损、拉 1m 线识别趋势（趋势一定有，强制方向）、只做合约、杠杆倍数可选、单笔金额=总仓位 1%、算手续费（止盈止损）、开单+止损止盈同挂（每单必挂 L1-4）。
- 新增文件：
  1. `scripts/scalper.py`：复用 market_scan 的 `_http_get/ema/atr/fetch_specs`。拉 1m 已收盘 K 线（默认 120 根）→ `detect_trend`（EMA9/21 排列 + 动量，**强制返回 long/short**，strength=strong/weak）→ ATR14 → 凯利公式 `kelly_rr`（`f=p-q/RR ⇒ RR=q/(p-f)`，f 取半凯利 0.16，趋势强 p=0.60/弱 p=0.52，RR 夹在 1~4）→ 止损距离=ATR×atrMult、止盈=止损×RR → 手续费（双边 fee_rate×2，止盈不足覆盖则放大）→ 输出信号+spec(ctVal/lotSz/minSz/tickSz)。只算信号不下单。
  2. `src/scalper.ts`：`ScalperSignal`/`ScalpResult` 类型、`fetchSignal`（调 scalper.py）、`scalpOnce`（信号→权益→名义金额=权益×riskPct→张数=名义/(ctVal×价) 按 lotSz 取整→setLeverage→placeOrder 市价→placeOco 止损止盈同挂→confirmAlgo 回查）。**已有该标的持仓则跳过**（等 OCO 平仓再开，避免高频堆仓）。
- 改动：
  - `store.ts`：`ScalperConfig` 接口 + `DEFAULT_SCALPER`（inst BTC-USDT-SWAP/杠杆5/riskPct0.01/atrMult1.5/feeRate0.0005/enabled false/intervalSec60）+ `StoreData.scalper` + defaults + loadStore 合并（`{...d.scalper,...raw.scalper}`）+ `getScalperConfig/updateScalperConfig`。
  - `main.ts`：`scalperTick()` 用 setTimeout 递归独立循环（每轮重读配置，intervalSec 动态生效；scalperBusy 防并发），与主轮次 while(true) 并行。ONCE 分支不启动。
  - `electron/main.ts`：`scalper:get/update/once` IPC（once 走 loadDist("scalper.js") 的 scalpOnce）；`preload.ts` 暴露 scalperGet/Update/Once。
  - `ui/App.vue`：加「超短线」页签（tabs + views + scalp 秒表图标）；`ui/store/index.js` store 加 scalper 状态 + loadAll 加载。
  - `ui/components/ScalperView.vue`：配置面板（启用开关/标的/杠杆下拉 1/2/3/5/10/单笔金额比例/ATR系数/手续费率/轮询间隔）+「立即开一单」（二次确认）+ 最近信号结果展示（方向/止损止盈/RR/凯利f/净止盈/净止损）。
- 关键设计/权衡：
  - 凯利公式用法 = 通过 `RR=q/(p-f)` 反推止盈/止损的盈亏比（趋势强 RR 低、弱 RR 高补偿）；止损距离仍用 ATR。
  - 杠杆「倍数可选」= UI 提供 1/2/3/5/10x（默认 5x），>5x 已标注「超过章程 L1-2，高风险」——用户明确要求可选，但默认守 L1-2。
  - 手续费：scalper.py 输出 net_tp_pct/net_sl_pct（净止盈/净止损，已扣双边手续费），止盈距离不足覆盖手续费时自动放大。
  - clOrdId roundId 用 `S${Date.now()}` 保证唯一（order_id.py 幂等键）。
- 验证：TS lint 0 错误、scalper.py py_compile 通过。
- 注意：`ui/App.vue` 在本会话期间已被并行重构（引入 nav.js/ReportsView/MarketView/WinFrame，tab 状态移到 lib/nav.js），我改 App.vue 时以实际文件为准（tabs/views/ICONS 都加了 scalp）。

## 超短线开单失败修复（argparse help 含 % 触发格式化错误）
- 现象：开单报 `Command failed: python scripts/scalper.py ... Traceback ... argparse.py _check_help → formatter._expand_help`。
- 根因：`scalper.py` 的 `--fee-rate` help 字符串里 `默认 0.05%` 含 `%`，Python argparse 会把 help 当 `help % dict` 做格式化，`%` 触发 `unsupported format character`。**此坑历史已踩过**（memory 2026-09-04「非技术 alpha 源探索」：argparse help 含 % 需 `%%` 转义或去掉 %）。
- 修复：`0.05%` → `0.0005`（去掉 %）。`python scripts/scalper.py --help` 已验证不再报错。
- 顺手优化（开箱即用）：
  1. `kelly_rr` 重写：原「固定 f_target=0.16 反推 RR」在胜率 0.6 时算出 RR=0.91 被夹到 1.0 → 止盈=止损距离，扣双边手续费后负期望。改为「保本盈亏比 RR0=q/p 翻倍，夹在 [1.5,4]」：strong(p=0.6)→RR1.5、weak(p=0.52)→RR1.85，保证期望为正。
  2. `atrMult` 默认 1.5→2.5（store.ts DEFAULT_SCALPER + scalper.py argparse default）：1 分钟 BTC ATR≈0.085%，atrMult 太小则止损距离 < 手续费占比，期望恒负；2.5 时 strong 期望≈+0.005%/笔（超短线真实水平，靠高频累积）。
- 验证：scalper.py 实跑输出正常信号（direction/sl/tp/RR/净止盈净止损）。

## 超短线板块：展示开仓记录/持仓/收益 + LLM 介入判向
- 需求：①把开仓记录、持仓、收益显示在超短线页 ②支持 LLM 介入，介入则趋势判断交给 LLM。
- 改动：
  1. `scripts/scalper.py`：out 加 `closes`（最近 60 根 1m 收盘价，round 2 位，供 LLM）、`sl_dist`/`tp_dist`（价格距离，方向无关）。
  2. `src/scalper.ts`：
     - `ScalperConfig.useLlm`（store.ts 定义，DEFAULT 加 useLlm:false）：true 时 `llmDirection(closes)` 用 `resolveModel()+createProvider` 调 LLM 判向（prompt 英文，输出 JSON `{"direction":"long"|"short"}`）；失败/mock 回退规则方向。方向按 `sig.sl_dist/tp_dist` 重算 sl/tp（方向可能被 LLM 翻转）。
     - 开单记录：`data/scalper_trades.jsonl` 只追加（ts/inst/direction/entry/sl/tp/size/leverage/rr/ctVal/judge/status）。judge=rule|llm 标注方向来源。
     - `syncTrades()` 平仓检测：open 记录对应标的已无持仓 → status=closed + 用 `mcpCall("demo","market_get_ticker")` 拉最新价近似平仓价 → pnl=(close-entry)×size×ctVal×dir（best-effort，拉价失败 pnl=null）。
     - `getScalperOverview()`：返回 {trades, positions(实时 fetchAccount), realizedPnl, unrealizedPnl}。
  3. `electron/main.ts` 加 `scalper:overview` IPC（loadDist("scalper.js").getScalperOverview）；`preload.ts` 加 scalperOverview。
  4. `ui/components/ScalperView.vue`：加「LLM 介入」开关；加收益卡片（总/已实现/未实现/开单笔数）+ 当前持仓表 + 开仓记录表（倒序，含判断来源 LLM/规则、状态、平仓价、盈亏）；`onMounted/onActivated` + 8 秒轮询 `loadOverview` 刷新。
- 关键设计：已实现收益用「平仓检测 + 最新价近似平仓价」而非查 OKX 账单（简化）；OCO 平仓实际价是 sl 或 tp，近似存在偏差，展示级够用。LLM 判向失败/mock 自动回退规则方向，不影响开单。
- 验证：TS lint 0、scalper.py 输出含 closes/sl_dist/tp_dist。

## 超短线循环不自动开仓修复（reloadStore 缺失）
- 现象：超短线循环未实现自动开仓。
- 根因：`main.ts` 的 `scalperTick()` 里直接 `getScalperConfig()`，而 `loadStore()` 有模块级 cache——agent 子进程启动时加载一次 store.json 后一直用内存缓存。UI 通过 electron 主进程 `scalper:update` 写 store.json，agent 子进程的 cache 不会自动更新。主轮次 `runRound()` 首行有 `reloadStore()` 所以能读最新配置，但 `scalperTick()` 漏了，导致：UI 里「启用」后 agent 仍读 enabled=false，循环永不触发开单。
- 修复：`scalperTick()` 开头加 `reloadStore()`（每次 tick 重读 store），使 UI 改配置 ≤ intervalSec 后生效。
- 附带修复：
  1. `scalper.ts` `scalpOnce()` 开头加 `--dry-run` 检查：演练模式下超短线不下单（与主轮次一致，避免 dry-run 误真单）。
  2. `main()` 启动时打印 `超短线循环已启动: enabled/inst/leverage/interval/useLlm` 日志，便于排查。
- 注意：改的是 agent 子进程代码（main.ts/scalper.ts），需 `npm run build` 重新编译 + 重启 agent（点「停止服务」再「启动服务」）才生效；仅重载 UI 不够。
- 教训：任何独立循环/定时器读 store 配置，都必须先 `reloadStore()`（store.ts 的 loadStore 有模块级 cache，跨进程不自动刷新）。

## 超短线循环监测记录展示
- 需求：超短的循环监测记录要显示一下。
- 改动：
  1. `src/scalper.ts`：新增 `ScalperTick` 接口（ts/inst/direction/strength/entry_ref/judge/result/opened|skipped|error/reason）+ `SCALPER_TICK_LOG=data/scalper_ticks.jsonl` + `appendTick`/`readTicks(limit=200)`；`scalpOnce` 重构为用局部 `finish(result, reason, signal?, judge?)` 统一收口——**每个返回点（dry-run/信号失败/无权益/已有持仓/缺spec/张数0/clOrdId失败/下单失败/成功）都先 appendTick 再 return**；`getScalperOverview` 加 `ticks` 返回。
  2. `ui/components/ScalperView.vue`：加「循环监测记录」面板（表格倒序：时间/方向/趋势/参考价/判断 LLM|规则/结果 已开单|跳过|错误/说明）。
- 说明：ticks 是「每轮监测」的记录（含跳过/失败原因），与 trades（只记成功开单）互补；UI 8 秒轮询 overview 自动刷新。

## 超短线独立启停按钮（循环移到 electron 主进程）
- 需求：超短的启动按钮是单独的，在页面上（独立于主轮次「启动服务」）。
- 改动：**把超短线循环从 agent 子进程（main.ts）移到 electron 主进程**，由「超短线」页签独立启停：
  1. `src/main.ts`：删除 `scalperTick`/`scalperTimer`/`scalperBusy` + `getScalperConfig`/`scalpOnce` import + main() 里的启动调用。agent 子进程只跑主轮次 runRound。
  2. `electron/main.ts`：新增 `scalperLoopTick`（withStore 读配置 → loadDist("scalper.js").scalpOnce → pushLog；`if (scalperLoopTimer !== null)` 才 schedule 下一次，用 setTimeout 递归）+ `startScalperLoop`/`stopScalperLoop`；新增 IPC `scalper:start`/`scalper:stop`/`scalper:status`（status 返回 {running: !!scalperLoopTimer}）。
  3. `electron/preload.ts`：暴露 scalperStart/scalperStop/scalperStatus。
  4. `ui/components/ScalperView.vue`：移除「启用超短线循环」复选框，改为「循环」行（运行中/已停止 tag + 启动循环/停止循环按钮）；save 不再传 enabled；onMounted 加 `refreshLoopStatus`。
- 语义变化：`ScalperConfig.enabled` 字段变死字段（循环启停由 scalper:start/stop IPC 控制，scalperLoopTick 不检查 enabled）。「启动服务」=只启动主轮次；「启动循环」=只启动超短线，两者完全独立。
- 注意：electron 主进程 withStore 的 store 模块实例缓存由 updateScalperConfig 的 saveStore 更新，故 scalperLoopTick 无需 reloadStore（与 agent 子进程不同）。

## 超短线「趋势反转平仓」勾选项
- 需求：循环时若判断趋势反转、有相反方向的单，要求平掉；用勾选框控制，勾选才做。
- 改动：
  1. `store.ts`：`ScalperConfig` 加 `closeOnReversal: boolean`，DEFAULT 加 `closeOnReversal: false`。
  2. `src/scalper.ts`：加 `positionSide(p)` helper（兼容 posSide=long/short + net 模式 pos 正负）；`scalpOnce` 顺序调整——**先判方向（规则/LLM）再处理持仓**：有持仓时 `ps !== direction`（趋势反转）→ 勾选 closeOnReversal 则 `closePosition`（平多 sell / 平空 buy，size=abs(pos)）后继续开新方向单，未勾选则跳过；`ps === direction` 则跳过（不重复开单）。
  3. `ui/components/ScalperView.vue`：加「趋势反转平仓」勾选框；save 加 closeOnReversal。
- 逻辑要点：平仓后**继续走开新单流程**（市价开新方向 + OCO 同挂）；平仓失败返回 error 不继续。

## 超短线趋势判断改用 5m 线
- 需求：非 LLM 判趋势至少用 5m 线（不能拿 2~3 根 1m 判断）；LLM 判趋势同样用 5m 线并明确告知模型。
- 改动：
  1. `scripts/scalper.py`：`fetch_candles_1m` → 通用 `fetch_candles(inst, bar, limit)`；`main` 拉两套线——**5m 线判趋势**（EMA9/21+动量，`closes5m`），**1m 线算 ATR/止盈止损**（`closes1m`）；`detect_trend` 动量窗口 6→10 根（`closes[-1]-closes[-11]`，需 >=12 根）；输出 `closes` 改为 `closes5m[-60:]`（供 LLM）；`bars`=len(rows5m)。5m 已收盘线 <30 报错。
  2. `src/scalper.ts`：`llmDirection` 系统提示词改为「given 5-minute closing prices，用 FULL series 至少最近 ~30 根判趋势，勿只看 2~3 根」；user 注明 5m closes + 根数。头部注释同步。
- 关键点：趋势方向与 ATR 数据源分离（5m 判向 / 1m 算止损），`entry_ref`/`last` 仍取 1m 最新收盘价。

## 更正：趋势判断改回 1m 线，用最近 5 根 1m 收盘价「看斜率」
- 用户澄清：不是用 5m 线，而是**仍用 1m 线**，用最近 5 条 1m K 线看斜率判趋势。
- 改动（覆盖上一条 5m 方案）：
  1. `scripts/scalper.py`：`import` 去掉 `ema`；新增 `linear_slope(closes, n=5)` 最小二乘斜率；`detect_trend(closes)` 改签名——`slope_pct = slope/现价`，`>0 → long`、`<0 → short`，`|slope_pct|>=0.0002 → strong` 否则 weak；`main` 只拉 1m 线（去掉 5m/rows1m 双线），`direction,strength=detect_trend(closes)`；输出 `closes=closes[-60:]`、`bars=len(rows)`。
  2. `src/scalper.ts`：`llmDirection` 提示词改为「given 1m closes，用最近 5 根 1m 收盘价的 SLOPE 判向（rising→long, falling→short），勿少于 5 根」。
- 约定：趋势方向 = 5 根 1m 斜率（规则与 LLM 一致）；ATR/止盈止损仍 1m。

## 超短线开仓记录补齐字段 + 日期筛选 + 总盈亏
- 需求：开仓记录缺单笔盈亏/费率等信息，补齐并算总盈亏，支持时间日期筛选。
- 后端 `src/scalper.ts`：
  - `ScalperTrade` 加 `feeRate`(单边)/`notional`(名义)/`margin`(保证金)/`fee`(双边手续费)/`netPnl`(净盈亏=pnl-fee)。
  - `ScalperOverview` 加 `realizedNetPnl`/`totalFee`。
  - `scalpOnce` appendTrade 写入 `notionalUsdt=size*ctVal*price`、`marginUsdt=notional/lever`、`feeUsdt=notional*(fee_rate??cfg.feeRate)*2`。
  - `syncTrades` 平仓算 `netPnl=(pnl??0)-(fee??0)`；`getScalperOverview` 汇总 `realizedNetPnl`/`totalFee`（closed 单）。
- 前端 `ui/components/ScalperView.vue`：
  - 卡片改 5 张：总净收益(realizedNetPnl+unrealized)、已实现净盈亏、未实现、总手续费、开单笔数(筛选后/总数)。
  - 开仓记录表加日期筛选(dateFrom/dateTo `type=date`，`filteredTrades` computed 按 ts 过滤) + 新增列：名义金额、保证金、费率、手续费、净盈亏。
- 兼容：旧 jsonl 记录无新字段，前端 `fmtNum(undefined)→"—"`、`feeRate??0`、`fee??0` 兜底；旧 closed 单 syncTrades 会重算 netPnl(费用按 0)。IPC `scalper:overview` 透传 `{ok,...r}` 无需改 main.ts。

## 修复：趋势反转平仓前先撤 OCO 止损止盈
- BUG：反转平仓只平仓位、没撤配套 OCO，残留止损单会反向触发。
- 改动：
  1. `src/okx.ts` 新增 `cancelAlgoOrders(inst)`：`swap_get_algo_orders`(status=pending, instId) → unwrap 过滤该 inst → 提取 algoId 列表 → `swap_cancel_algo_orders`({orders:[{algoId,instId}]}, allowWrite)。返回 {ok, canceled, raw}。
  2. `src/scalper.ts`：import 加 `cancelAlgoOrders`；`scalpOnce` 反转平仓分支——**先撤 OCO（失败即 error 停止）→ 再 closePosition**。
- 顺序关键：撤单失败不能继续平仓（否则残留 OCO）。撤单 canceled=0（无 pending）视为成功继续。

## 超短线历史回测功能
- 需求：手动选区间、拉 1m 数据、回测超短线策略、生成详细记录。
- 新增 `scripts/scalper_backtest.py`（与 scalper.py 策略严格一致：5 根 1m 斜率判向 + Wilder ATR(14) 止损 + 凯利 RR 止盈 + 双边费率；可选 --close-on-reversal；信号用已收盘 K 线→下一根开盘价入场，无未来函数）。
  - **关键坑**：OKX `/market/history-candles` 用 `after` 游标向前翻页（返回该时间戳「之前」更旧数据），与 `/market/candles` 的 before 语义相反；用 before 会返回最新数据导致 0 根。
  - 输出：`{inst,start,end,params,bars,summary{...},trades[每笔 n/side/entryTs/exitTs/entry/exit/sl/tp/bars/reason/rr/pnlPct/feePct/netPnlPct/pnlUsdt/feeUsdt/netPnlUsdt]}`；summary 含 trades/longs/shorts/winRate/totalNetPnlUsdt/totalNetPnlPct/profitFactor/maxDrawdownPct/sharpe/totalFeeUsdt/avgBars。
  - 时间解析按 CST（+8），输入 "YYYY-MM-DD[ HH:MM:SS]"。
- 后端 `src/scalper.ts` 加 `runScalperBacktest(args)`（runPy 调脚本，180s 超时）；`electron/main.ts` 加 `scalper:backtest` IPC（返回 `{ok,!error,...r}`）；`preload.ts` 加 `scalperBacktest`。
- 前端 `ui/components/ScalperView.vue` 加「超短线回测」面板：datetime-local 起止区间（前端 replace("T"," ")+":00" 转后端格式）+ ATR×/费率/名义USDT/趋势反转勾选；结果 = 8 张汇总卡片 + 回测明细表（每笔开平时间/价/止损止盈/持仓分钟/原因/盈亏/手续费/净盈亏）。
- 验证：py_compile OK；tsc（src/electron/preload）均 0 错误；实际跑 2h 区间产出 6 笔交易 JSON 正常。

## 修复：开仓记录一直显示「持仓中」
- 根因：`syncTrades` 用 `posInst.has(inst)`（标的是否还有持仓）判平仓。同标的连续开单时（趋势反转平仓→开新单、止损后重开同向），旧 open 记录因「标的仍持仓」永远不关闭，一直显示持仓中。
- 修复 `src/scalper.ts`：
  1. `syncTrades` 重写：按标的分组 open 记录，ts 升序，只有「最新一条 + 方向匹配当前持仓」保持 open，其余（旧单/方向不匹配/标的已无持仓）用 `fetchLastPrice` 近似价关闭。
  2. 新增 `closeOpenRecords(inst)`：开新单前立即关闭该标的全部旧 open 记录（及时更新，不等轮询）。
  3. `scalpOnce` appendTrade 前调用 `await closeOpenRecords(cfg.inst)`。
- 验证：lint 0、tsc 0 错误。

## 从 SkillForge 投资分类内置 3 个 Skill 知识
- 需求：从 https://skills.yangsir.net/domains/investment 找适合本项目的 skills 下载并内置。
- 判断：该站 skill 多为 Claude Code 的 SKILL.md（给 AI 的指令 prompt），依赖外部 CLI/MCP/付费 API key，不能直接跑。真正可落地的是「知识/方法论型」，故转成项目原生的**专家知识库文档**（`experts/<id>/knowledge/*.md`，运行时自动注入该专家 systemPrompt，零外部依赖）。
- 内置 3 个（均中文精炼要点、标注来源与作者）：
  1. `experts/funding/knowledge/01-衍生品情绪解读.md` ← coinglass skill（资金费率分档/OI×价格矩阵/多空比/清算/CVD 解读阈值）
  2. `experts/factor/knowledge/01-回测方法论.md` ← backtesting-frameworks skill（回测五大偏差/三段结构/walk-forward，补 MEMORY 过拟合教训）
  3. `experts/trading/knowledge/01-聪明钱信号解读.md` ← okx-cex-smartmoney skill（OKX 聪明钱共识信号解读，标注项目 MCP 暂无聪明钱 API）
- 未内置及原因：coinglass 工具部分（需 $699/月 API key）、okx-cex-trade/portfolio/bot 等（项目已有 OKX MCP 覆盖）、ljg-invest（价值投资，与 5 分钟永续短线关联低）。
- commit：ab5aa53（已用 UTF-8 消息文件 + `i18n.commitEncoding=UTF-8` 提交，hex 校验 e58685e7bdae=内置 正确无乱码）。

## 超短线「回查=false 但实际成功」bug 修复（MCP 返回剥层少一层）
- 现象：每次动作返回 `[超短线] ... OCO=true 回查=false`，整体被判 error，但实际 OCO 止损止盈已挂成功。
- 根因：`okx.ts` 的 `confirmAlgo` 与 `scalper.ts` 的 `fetchLastPrice` 用 `Array.isArray(d?.data)` 只剥**一层** data 解析 MCP 返回。而 mcpCall 返回是三层洋葱 `j.result.data.data`（`fetchAccount` 里 `rowsOf` 注释早写明「三层，别再数错」，循环 3 次剥 data）。剥一层拿到的是 `{endpoint,requestTime,data:[...]}` 对象而非数组 → arr 恒空 → `confirmAlgo` 永远找不到 instId 返回 false；`OCO=true` 来自 `placeOco` 的 `r.ok`（不涉及深层解析），故表现为「挂单成功但回查失败」被误判 error。
- 修复：两处改用现成的 `unwrap(r.data)`（`unwrap` 剥两层 data，正好等价 rowsOf 的三层）。scalper.ts 顺带 import `unwrap`。
- 教训：**凡手动解析 mcpCall 返回，必须用 `unwrap()`，不要手写 `d?.data` 一层剥**。项目里洋葱层数已有三处实现（unwrap 两层 / fetchAccount.rowsOf 三层循环），语义等价，别再各写各的。
- 验证：TS lint 0 错误。

## 超短线「一键平仓」按钮
- 需求：超短线板块加一键平仓按钮。
- 实现链路（4 文件）：
  1. `src/scalper.ts` 新增导出 `closeScalperPositions(inst?)`：目标标的 = 开仓记录 `status=open` 的 inst 集合 ∪ 传入的当前配置 inst（兜底）→ `fetchAccount` 筛出这些标的的有效持仓 → **每个标的先 `cancelAlgoOrders` 撤 OCO 再 `closePosition` 市价平仓**（沿用 scalpOnce 趋势反转平仓既有套路，残留止损单会反向触发）→ 平完 `syncTrades()` 把记录 open→closed 并算近似盈亏 → 返回 `{ok, msg, closed}`，部分失败时返回 ok=closed>0 并带失败明细。
  2. `electron/main.ts` 加 `scalper:closeAll` IPC（`withStore` 读 cfg 拿 cfg.inst → `loadDist("scalper.js").closeScalperPositions`）。
  3. `electron/preload.ts` 暴露 `scalperCloseAll`（api.js 的 buildApi 自动枚举 window.api 属性，前端直接 `api.scalperCloseAll()`）。
  4. `ui/components/ScalperView.vue`：`closing` ref + `closeAll()`（`ask()` 二次确认 danger → 调 api → toast → `loadOverview()`）；按钮放「当前持仓」面板 h2 右侧（`<span class="spacer">` + `class="danger"`，无持仓时 disabled）。
- 设计要点：**平仓范围限定在超短线自己的 open 记录标的，不碰主轮次仓位**（避免误平）；单标的失败不中断其余标的平仓（收集 errs 继续）。
- 验证：TS lint 0 错误 + `pnpm run build` 全量编译通过（tsc×3 + vite）。
- 注意：改 src/ 后必须 `pnpm run build`，因 electron 主进程用 `loadDist("scalper.js")` 加载 dist 产物，仅重载 UI 不生效。
