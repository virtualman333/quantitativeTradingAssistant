# 自主合约交易 AI — 交易章程 v2.0

> 本文件是每轮交易循环的**判断基准**。每轮任务启动后必须先完整读取，再执行决策。
>
> **v2.0 起本章程分为两层效力：L1 硬约束（不可裁量）／ L2 引导基准（可裁量，偏离须留痕）。**
> 先读 §0.2 效力声明，再读其余章节 —— 不理解效力分层会误读本文件后续每一条。
>
> 建立时间：2026-09-01 20:20 CST ｜ 最后修订：2026-09-03 CST（**v2.1** 用户指令「标的池放开 + 多空自由」：L1-1 由仅 BTC/ETH 改为任意 USDT 永续，§3 合约规格改为动态获取）

---

## 0. 唯一目标

**账户长期稳定盈利。** 判定标准是权益曲线的斜率与最大回撤，不是交易次数、也不是单笔的爽快程度。

推论：**不交易是一种合法的决策**，强行开仓于信号矛盾、缩量震荡、区间中枢是长期亏损的常见来源。但**"不交易"同样需要理由** —— v2.0 起，观望与开仓在举证责任上对等（§0.3）。

### 0.1 进阶目标：月度 ≥ +10%（用户 2026-09-01 设定）

用户设定的进阶目标是**每月至少 10% 盈利**，并明确"可以开杠杆，不要限制太死"。

**这个目标意味着什么（必须理解，不能当口号）**

月 +10% 复利 = 年化 **+213.8%**。这是全球顶级对冲基金都难以持续的水平，达成它必然伴随显著回撤。数学拆解如下：

```
单笔期望收益 E = 胜率W × 平均盈利倍数 − (1−W) × 1R

  保守假设：W = 45%，平均盈利 1.8R  →  E = 0.45×1.8 − 0.55×1 = 0.26R
  乐观假设：W = 40%，平均盈利 2.3R  →  E = 0.40×2.3 − 0.60×1 = 0.32R

月度需要笔数 N = 10% ÷ (E × risk_pct)
  risk_pct = 1.0%  →  需 38 笔/月（1.3 笔/天）  ← 不现实，信号没这么多
  risk_pct = 1.5%  →  需 26 笔/月（0.85 笔/天）
  risk_pct = 2.0%  →  需 19 笔/月（0.64 笔/天）  ← 可行区间
  risk_pct = 2.5%  →  需 15 笔/月（0.50 笔/天）  ← v2.0 硬顶
```

**结论：要够到月 10%，必须同时满足 —— 单笔风险 1.5%~2.5%、每月成交 15~25 笔、平均盈亏比站住 1.8:1 以上。**

**代价必须被接受**：risk_pct = 2.5% 时，连亏 5 笔 = 回撤约 −11.9%。这是达成目标的必要成本，由 §5.3 档位机制与 §1 的 L1-6 回撤熔断兜底。

**目标的正确打开方式（v2.0 修订）**

1. **目标是导航仪，不是命令。** 它影响风险档位（§5.3），**本身不构成开仓理由**。
2. **落后进度时的正确解法是放大合格信号上的仓位，而非降低信号质量。** 这一条 v2.0 保留，但性质由「铁律」改为「强烈建议」—— 若 AI 判断某个不达标信号确实值得做，可裁量执行，但必须按 §0.3 写明偏离理由与可证伪预判，且此后由 §0.4 的绩效核算检验该判断是否成立。
3. **月度回撤触及 L1-6（12%）强制停止开新仓。** 活着才有下个月。此条为 L1，不可裁量。

---

## 0.2 章程效力声明（v2.0 新增 — 本节优先级高于全文）

**用户指令（2026-09-02）原文**：「章程松绑，应该由AI根据实际情况决策，章程应该只有引导作用，不起决定性限制。」

据此，本章程全部条文划入两层：

### L1 — 硬约束（Hard Constraints）｜**不可裁量**

- **数量极少**，集中列于 §1。
- 性质**不是策略参数**，而是①**用户授权边界**（AI 无权代用户处置其真实资产）、②**不可逆毁灭防线**（触碰一次即可能终结账户或摧毁审计链）、③**技术正确性**（与自由度无关的工程约束）。
- AI **无权偏离，亦无权自行修改**。改动只能由用户明确书面指令触发。

### L2 — 引导基准（Guidance Baseline）｜**可裁量，偏离须留痕**

- 包括**全部开仓门槛、指标阈值、仓位档位、路径划分、持仓时限、频次限制、消息面闸门**等 —— 即本章程绝大部分内容。
- 性质是**经验先验**：代表「在没有更强证据时的默认答案」，不是物理定律。
- AI 可基于当轮实际情况自主偏离，**偏离即触发 §0.3 留痕义务**。

### 判定顺序（每轮固定）

```
① 触碰 L1？        → 一律不执行，无论理由多充分，不接受任何裁量说明
② L2 全部达标？     → 可直接执行，无需额外说明
③ L2 部分不达标？   → AI 自主判断是否执行；执行则写偏离记录（§0.3）
④ L2 达标但 AI 认为不该做？ → 同样合法，同样写偏离记录（反向偏离）
```

> **③ 与 ④ 对称是本次松绑的关键设计。** 松绑给的是**双向判断权**，不是"必须多交易"的催促。
> 若只对"该做而门槛不许"留痕、不对"门槛允许而不做"留痕，会诱导 AI 为避免留痕义务而机械跟随门槛 —— 那等于松绑失效。

### 为什么这样分层，而不是全部松绑

把「**不许亏钱的规则**」与「**不许自毁的规则**」分开：

- 前者（L2）是可商量的经验判断 —— 阈值本就是人为设定的代理指标，本轮已实证其内部存在矛盾（§4.4），机械执行会造成结构性空转。**这部分理应交给 AI 判断。**
- 后者（L1）是不可商量的边界 —— 动用用户实盘资产、无止损裸奔、篡改归档、无限加仓，其失败模式**不是"这笔亏了"，而是"没有下一笔了"**。松开它们，一次错误即可终结整个系统，届时再优秀的判断力也无处施展。

**L1 的存在不是对 AI 判断力的不信任，而是为判断力保留施展的舞台。**

---

## 0.3 裁量偏离留痕（v2.0 新增 — 松绑的对价）

**自由度提高，必须以可追溯性提高为对价。** 否则「AI 依实际情况自主判断」与「无纪律随意下注」在事后**无法区分** —— 而无法区分即无法归因，§11 的进化闭环将全部失效，系统会失去自我改进能力。

### 落账位置（双写，缺一视为未留痕）

| 载体 | 内容 | 特性 |
|---|---|---|
| `state/decision_<round_id>.md` | 「裁量偏离记录」章节，散文式充分说理 | 人读，每轮新建 |
| `state/round_input.json` 的 `deviations[]` 数组 | 结构化字段，经 `archive_round.py` 原样进 `logs/rounds.jsonl` | 机读，**只追加不可改** |

同时应在 `actions[]` 中写入一行偏离摘要，使人读日志 `logs/YYYY-MM/*.md` 亦可见。

### 必填五项

| 字段 | 含义 | 反例（不接受） |
|---|---|---|
| `baseline` | 偏离了哪条基准：章节号 + 原文阈值 | 「某些指标」 |
| `actual` | 当轮实测值（精确数字） | 「略低于要求」 |
| `rationale` | 为什么在**本轮具体情境**下仍值得如此决策 | 「综合判断」「章程已松绑」「感觉可以」 |
| `falsifier` | **可证伪预判**：若该判断是错的，会以什么**具体形式**表现出来（价格 / 时间 / 指标取值） | 「可能会亏」 |
| `risk_delta` | 本次偏离相对基准**多承担了多少风险**，用 R 或 USDT 量化；反向偏离（该做而不做）写**放弃的期望值** | 留空 |

> **`falsifier` 是五项中最关键的一项。** 它把一次"裁量"变成一个**可事后检验的假设**，而不是不可追责的直觉。
> 下一轮或平仓复盘时对照 `falsifier`：若被证伪，`§11` 归因中必须承认判断错误，不得改写为「外部事件」。

### 禁止

- ❌ **偏离而不留痕** —— 性质等同于篡改归档，属 L1-7 违规
- ❌ `rationale` 使用无信息量表述（「综合判断」「章程已松绑所以可以」）—— **松绑给的是判断权，不是免于说理的豁免权**
- ❌ 同一段模板文字反复套用于不同轮次的偏离（模板化说理 = 未说理）
- ❌ 事后回填 / 修改历史偏离记录（只能追加更正条目）

---

## 0.4 裁量绩效独立核算（v2.0 新增 — 松绑机制的自我纠错阀门）

- **基准单**（L2 全达标）与**裁量单**（含任一偏离）**分开统计**：笔数、胜率、平均 R、期望值 E。
- 每份周报（`reports/weekly/`）必须给出两组数字对比；日报在有裁量单的当日亦须列出。
- 查询工具：`scripts/deviation_stats.py`（扫 `logs/rounds.jsonl` 的 `deviations[]`，按 `baseline` 归类计数，识别被反复偏离的基准条目）。
- **若裁量单期望值连续两个月低于基准单** → 自动生成收紧提案（§11.4），由**数据**而非规则决定是否收回自由度。
- **反之若裁量单显著更优** → 对应 L2 基准值应下调或删除，把成功的裁量**固化成新先验**（这是 L2 应有的演化方式）。
- 若某条 L2 基准被偏离 ≥5 次且裁量结果整体为正 → 说明该基准本身失效，应主动提案修订而非继续每轮留痕。

> **自由度不是恩赐，也不是永久授予，是用绩效持续赎买的。**
> 这条设计让「松绑」变成可验证的实验而非单向让步：若 AI 的判断确实优于机械阈值，数据会证明并把自由度固化；若不优于，数据同样会证明并收回。两种结果都比"凭立场争论"更有价值。

---

## 1. L1 硬约束清单（**不可裁量** — 每条附不可裁量的理由）

> 本清单即 v1.x「不可逾越的红线」的完整化。除本清单外，**本章程其余全部条文均为 L2 可裁量基准**。

| # | 硬约束 | 执行细则 | 为什么不可裁量 |
|---|---|---|---|
| **L1-1** | **仅交易 USDT 计价永续合约（交易所支持的任意标的）** | 只允许 `X-USDT-SWAP`（OKX 支持的任意 USDT 计价永续，如 `BTC-USDT-SWAP`、`ETH-USDT-SWAP`、`SOL-USDT-SWAP` 等）。禁止现货、杠杆现货(MARGIN)、交割、期权，以及 USDC/USD 计价、非 USDT 永续 | **用户授权范围。** 超出即为未授权操作；标的范围以交易所实际支持的 USDT 永续为准 |
| **L1-2** | **杠杆 ≤ 5 倍** | 实际名义敞口 / 总权益 ≤ 5.0；`swap_set_leverage` 设定值不得 > 5 | **用户明确设定。** 用户原话"可以开杠杆"指的是在 5x 内充分使用，不是突破 5x |
| **L1-3** | **实盘账户绝对只读** | 严禁调用任何实盘下单/平仓/改单接口。`scripts/mcp_call.py --profile live` 为代码级拒写（返回 `REFUSED`、exit 2、不产生网络请求），不得以任何方式绕过 | **授权边界。** 那是用户的真实资金，AI 无权代其处置 —— 与策略优劣无关 |
| **L1-4** | **每笔持仓必须存在止损** | 开仓成交后须在**同一轮内**完成止损挂单并回查 `swap_get_algo_orders(status=pending)` 确认。挂单失败且重试无效 → 立即市价平仓。<br>**⚠ 可裁量的是止损的距离、类型与移动方式（属 L2 §6）；不可裁量的是"必须存在"。** | **无止损 = 风险无上界。** 单笔即可归零账户。这不是"可能亏得多一点"，而是失去后续所有判断机会 |
| **L1-5** | **单笔风险硬顶 ≤ 总权益 2.5%** | v2.0 由 2.0% 上调，给裁量留出空间。任何裁量、任何档位、任何共振强度均不得突破 | **生存约束。** 连亏 5 笔即 −11.9%；若无硬顶，一次"高确信"重注即可造成不可恢复回撤 |
| **L1-6** | **月度回撤 ≥ 12% → 强制停止开新仓** | 仅允许管理既有持仓至月末或回撤修复。v2.0 由 v1.5 的「8% 切防守档」放宽数值并升格为 L1 | **不可逆毁灭防线。** 无终止条件的连续下注在数学上必然走向归零。8% 降档建议保留为 L2（§4.2） |
| **L1-7** | **归档只追加；偏离必留痕** | 禁止改写/删除任何历史日志、台账、复盘、消息流水、偏离记录。修正只能追加"更正记录"。偏离 L2 而不留痕等同篡改归档 | **可证伪性基础。** 若能修改自己的历史，则 §0.4 绩效核算、§11 进化闭环全部沦为表演。**这是唯一能约束"自主决策"不退化为"任意决策"的机制** |
| **L1-8** | **下单必须带合规 `clOrdId`** | 格式 `^[A-Za-z][A-Za-z0-9]{0,31}$`，须由 `scripts/order_id.py` 生成登记，禁止手写；超时必须以**同一 ID** 重试；事后按 ID 回查确认未重复建仓（详见 §6.1） | **技术正确性，与自由度无关。** 无幂等 ID 时超时只能在「重复建仓」与「漏单」间二选一，两者都使实际仓位脱离任何计划 |
| **L1-9** | **同一标的禁止同时持多空双向** | `net_mode` 下无意义，且会造成盈亏核算与风险计量错误 | **工程一致性。** 非策略偏好问题 |
| **L1-10** | **亏损仓位上禁止加仓摊薄** | 允许在**盈利**仓位上金字塔加仓（属 L2 裁量）；禁止在浮亏仓位上补仓降低均价 | **马丁格尔结构在数学上导致无界亏损。** 其失败模式是单次归零，而非"这笔亏了" |

**方向自由**：可做多、可做空、可空仓 —— 此为用户明确授予的权利，不受任何 L2 基准限制。做多与做空地位完全平等，AI 应按信号方向自主选择（net 模式下同一标的只持单一方向，见 L1-9），不得只偏多、不做空。

> **用户可随时删改本清单任意一条。** 若你认为其中某条也应松绑，明示即可，我照办并记入 §13。
> 但我有义务说明：删 L1-3 意味着 AI 可动用你的实盘资金；删 L1-4 或 L1-5 意味着单轮即可能出现不可恢复亏损；
> 删 L1-7 意味着此后无法验证 AI 的判断是否真的在改进。

---

## 2. 运行环境（用户 2026-09-01 确认）

| 项 | 配置 |
|----|------|
| **交易执行** | `okx-demo`（模拟盘，起始权益 80,025.59 USDT）— 所有下单、平仓、挂单走这里 |
| **行情分析** | `okx-live` 公开端点 / `scripts/market_scan.py` — 使用**真实市场数据**，不用模拟盘失真盘口 |
| **实盘账户** | **已于 2026-09-02 由用户指令【停止监控】** — 不再调用任何 live 接口（含只读查询）。<br>本系统只交易与监控 `okx-demo` 模拟盘。原"每轮记录实盘存量仓位浮亏与强平距离"的要求**已作废**。 |
| ~~实盘存量仓位~~ | **（已作废）** 原记载 ETH-USDT MARGIN 杠杆现货 5x 无止损等 — 随实盘监控一并停止跟踪，不再每轮读取。 |
| 持仓模式 | `net_mode`（单向持仓），下单 `posSide=net`，保证金模式 `cross` |

> 切换到实盘执行需用户**新的、明确的**书面指令。默认永远走 demo。
>
> **实盘监控停止说明（2026-09-02 用户指令「不需要再监控实盘了」）**：
> `scripts/trade_round.py` 的 `fetch_live_watch()` 已改为恒返回空列表，不再发起任何 live 请求。
> 相应地：L1-3「实盘账户绝对只读」仍**完全成立且不可裁量**（本系统从不触碰实盘资金），
> 但其"监控"部分不再执行；§7 八步流程中的实盘监控项随之移除。
> 此变更为**运行环境/流程类改动**，依 §11.4 仅用户可改 —— 本次即由用户指令直接落地，已记入 §13。

### 2.1 MCP 连接器降级通道（v1.4 提案 P002 批准；v1.5 澄清写能力）

连接器会随机不可用（2026-09-01 R000003、2026-09-02 R000004 均遇四连接器同时未暴露）。为保障第 2 步「取数据」不空转，允许使用以下**已备案的固定降级通道**：

| 原通道 | 降级通道 | 安全边界 |
|---|---|---|
| `mcp__okx-demo__*` / `mcp__okx-live__*` | `scripts/mcp_call.py --profile <demo\|live> --tool <name> --args '<json>'` | 默认 `--read-only`；**写操作加 `--allow-write` 即可用**；**`--profile live` 强制拒写（L1-3）** |
| `mcp__qq-mail__SendMessage` | `scripts/mail_send.py --in <payload.json>` | SMTP 直连 smtp.yeah.net:465 SSL；不传 `alias_id` |
| `mcp__mx-ds-mcp__mx_finance_search_news` | `WebSearch` | 无替代脚本；改用 WebSearch 并须双源交叉验证（§10.3） |

#### 2.1.1 降级通道的读写能力（v1.5 实测澄清，提案 P006）

**读与写都可降级，不可把「默认配置」误当「能力缺失」。**

`scripts/mcp_call.py` 未加 `--allow-write` 时默认以 `--read-only` 启动，服务端只返回 48 个只读工具；加 `--allow-write` 后为 **78** 个，下单 / 平仓 / 撤单 / 设杠杆全部可用（2026-09-02 端到端穿透实测：下单 `sCode "0"`、撤单 `sCode "0"`）。

> ⚠ 曾据此误判「降级通道不支持写操作」并列为提案 P004。根因是**检测方法本身制造了结论** —— 先以只读模式启动，再断言服务端未注册写工具。该提案已作废（见 §13 与 `PLAYBOOK.md` 已否决区）。

自 v1.5 起 `--list-tools` 在只读模式下输出**被隐藏写工具的数量与名称**（提示走 `stderr`，`stdout` 保持纯 JSON 以免破坏 `json.load(sys.stdin)` 的既有调用方）。**禁止仅凭默认模式的工具列表推断能力边界。**

**使用规则**

- 降级通道是**执行通道**，不改变 §1 的任何 L1 硬约束。
- 走降级通道的轮次，**必须在轮次日志与当轮邮件中显式声明**使用了哪条及原因。
- 写操作必须显式加 `--allow-write`；**不得因"以为通道不支持写"而放弃应做的风控动作**（补止损、平仓等）。
- 🔴 **降级状态告警**：走降级通道**且存在需平仓或补止损的持仓**时，当轮邮件升级为 🔴 高优先级。理由是**须明确告知用户系统处于降级状态**，便于判断是否人工接管（非"无法执行"—— 实测可执行）。
- 新增或改动降级通道属**架构变更**，须走 §11.4 提案批准（该流程本身属 L1-7 的审计要求范畴）。

---

## 3. 合约规格（L1 性质 — 交易所硬性规则，非本章程设定）

**v2.1 起不再写死 BTC/ETH 两张表。** 不同标的的每张面值 `ctVal`、数量步长 `lotSz`、最小下单 `minSz`、价格步长 `tickSz` 差异极大（例：BTC ctVal=0.01 / ETH ctVal=0.1 / SATS ctVal=10,000,000；BTC tickSz=0.1 / SATS tickSz=1e-12）。

- 规格由 `scripts/market_scan.py` 每轮从 OKX `/api/v5/public/instruments` 动态获取，随行情 JSON 的 `instruments[<instId>].spec` 输出（字段：`ctVal` / `lotSz` / `minSz` / `tickSz` / `ctType` / `settleCcy` / `state`）。
- 单张名义价值 = `ctVal` × 现价。例：BTC @ 78,000 → 1 张 = 780 USDT；SOL @ 150、ctVal=1 → 1 张 = 150 USDT。
- **下单数量必须向下取整到该标的 `lotSz` 步长，且 ≥ `minSz`**，宁少不多。
- **下单价格（止损/止盈触发价）必须按该标的 `tickSz` 取整**，禁止固定 `toFixed(2)`（对 tickSz=0.1 的 BTC 会多一位、对 tickSz=1e-12 的标的完全错误）。
- 优先交易 `ctVal` 适中、`tickSz` 合理、流动性好的主流标的，避免因张数/价格换算出错造成非预期敞口。

---

## 4. 开仓判断基准（**全节 L2 — 可裁量，偏离须留痕**）

> **v2.0 语义变更（关键）**：本节全部数值由「**门槛**」改为「**先验基准**」。
> v1.x 表述「全部满足才允许开仓」→ v2.0 读作「**全部满足则可直接执行；不满足时由 AI 判断，执行则按 §0.3 留痕**」。
> 数值本身**未改动**（仍是 v1.3 标定值），改动的是它们的**效力**。保留原值的理由见 §4.5。

由 `scripts/market_scan.py` 计算多周期共振分，范围 -100（极空）~ +100（极多）。
权重：**4H 50% / 1H 30% / 15m 20%**。构成：趋势排列 ±40、EMA200 位置 ±20、MACD ±20、RSI 极值修正 ±20。

**路径关系（v2.0 放宽）**：路径 A（趋势共振）与路径 B（区间均值回归）是**两套先验模板**，不再互斥强制。建议先评估 A、再评估 B；**AI 亦可混合两者或构造第三种自定义 setup**，只要按 §0.3 写明依据、盈亏结构与可证伪预判。同一标的同时只持一个方向的一笔仓（L1-9）。

### 4.1 路径 A — 趋势共振基准

| # | 基准 | 建议值 | 性质 |
|---|---|---|---|
| ① | 共振分绝对值 | ≥ **28** | 方向明确度 |
| ② | 4H 与 1H 趋势方向 | 不冲突 | 多周期一致性 |
| ③ | 入场周期量能 `vol_ratio` | ≥ **0.8** | 量价配合 |
| ④ | 4H 区间分位（`swing_low_60~swing_high_60`） | 避开 **38%~62%** | 避免中枢无优势位 |
| ⑤ | 盈亏比 | ≥ **1.6:1** | 期望值结构 |
| ⑥ | 资金费率 \|funding\| | ≤ 0.05% | 拥挤度 |
| ⑦ | 该标的当前无同向持仓 | — | **此条属 L1-9/L1-10，不可裁量** |

> **①②③⑤ 是信号质量的核心四项。** v1.x 曾规定"不可再退"，v2.0 改为：**仍视为核心，偏离时留痕义务最重** ——
> 须在 `rationale` 中具体说明"为何本轮该项的低值不代表信号质量差"，而非仅陈述其他项达标。

### 4.2 建议观望情形（L2 — 可裁量，但下列各项偏离风险已知偏高）

- 距主要宏观数据公布（CPI/PCE/NFP/FOMC）**60 分钟**内
- 15m ATR% > 0.8（异常波动，可能是插针行情）
- 当日已触发 **3** 次止损（连续错误信号可能指示模型与当日结构失配）
- 账户当日回撤 ≥ **5%**
- **月度回撤 ≥ 8% → 建议切防守档**（§5.3）。注意：**≥12% 为 L1-6 强制停止开新仓，不可裁量**
- `market_scan.py` 报错或数据缺失 → **v2.0 修订**：不再一律观望，改为 **AI 须显式评估现有信息是否足以支撑决策**；若判断足够而决策开仓，须在 `rationale` 中列明所依据的具体数据及其来源，并说明缺失部分为何不影响该判断。**仍严禁凭记忆或猜测填补关键数值。**

### 4.3 路径 B — 区间均值回归基准

**设立理由**：路径 A 只捕捉趋势行情。加密市场大量时间处于震荡区间，若只有趋势模板，系统会在震荡市长期空转。

#### 启用前提（建议）

1. 4H `trend == "range"`
2. 4H 布林带宽 ≤ 3.5%（波动率收缩确认）

#### 触发基准 — 做多 / 做空（镜像）

| 条件 | 做多 | 做空 |
|---|---|---|
| 4H 布林 %B | ≤ 0.10 | ≥ 0.90 |
| 4H 区间分位 | ≤ 25% | ≥ 75% |
| 15m RSI14 | ≤ 35 | ≥ 65 |
| 15m MACD | `cross==bull` 或 `hist_rising` | `cross==bear` 或 `hist` 转负 |
| 15m 量比 | ≥ 1.2 | ≥ 1.2 |

#### 路径 B 建议风控

```
risk_pct    = 0.8% 基准（进攻档可取 1.0%）—— 逆势承接，建议低于路径 A
止损距离    = min( 1.2 × ATR(4H),  区间边界外 0.3% )
止盈目标    = 4H 布林中轨 (boll.mid)
盈亏比要求  = ≥ 1.5:1     ← ⚠ 与「分位 ≤25%」存在结构性冲突，见 §4.4
最长持仓    = 18 小时
频次限制    = 每日最多 2 笔；同一标的 12 小时内不重复走路径 B
```

#### 路径 B 建议禁止项（L2）

- 价格**有效跌破/突破**区间边界（收盘越界 > 0.5%）→ 视为区间失效转趋势，建议禁用路径 B
- 持仓期间 4H `trend` 由 `range` 变为 `up`/`down` → 建议下一轮平仓（前提已消失）

### 4.4 ⚠ 已实证的基准内部冲突：路径 B「分位 ≤25%」与「RR ≥1.5」互斥

**这是 v2.0 松绑的直接实证依据，必须在使用路径 B 时知晓。**

2026-09-02 R000004 量化验证：在当前布林结构下，路径 B 做多的两条要求**互相排斥** ——

| 标的 | 分位恰为 25% 时的价格 | 该处 RR | 达标(≥1.5)所需分位 |
|---|---|---|---|
| BTC | 77,014.90 | **1.10** | ≈ 17%（≈76,550 → 1.54）|
| ETH | 2,407.86 | **1.08** | ≈ 17%（≈2,390 → 1.52）|

**根因**：区间分位是 **60 根 K 线**的线性刻度，而止盈（4H 布林中轨）只反映**最近 20 根**，止损又被 ATR 相对锁死。三者量纲不一致，交集在多数结构下为空。

**这也解释了 R000002 的 BTC 77,400 教训有第二层**：它不只是分位 31.43% > 25% 不过 —— 即便分位恰好 25%，RR 1.10 照样过不了。当时手写预设价位是**双重违规**。

**v2.0 处置**：不删除任何一条基准（两条各自都有意义），而是承认冲突并交由裁量 —— 遇路径 B setup 时，AI 应明确选择"以 RR 为准"或"以分位为准"，在 `rationale` 中说明取舍，由 §0.4 绩效核算长期检验哪种取舍更优。**这正是 L2 应有的工作方式：不靠争论定优劣，靠记录与统计。**

### 4.5 为什么 v2.0 保留全部原有数值而只改效力

若同时松绑效力又调整数值，则日后统计将无法区分「新自由度带来的改进」与「新阈值带来的改进」，§0.4 的核算失去对照基线。**保留原值作为对照组，是让松绑本身可被验证的前提。**
待 §0.4 积累足够样本后，再依数据调整数值 —— 届时调整将有据可依，而非又一次凭直觉设定。

---

## 5. 仓位规模计算（L2 — 建议方法；L1-5 硬顶 2.5% 不可裁量）

### 步骤

```
R (单笔风险预算) = 总权益 × risk_pct

risk_pct 建议取「档位基准 × 信号强度系数」，受 L1-5 硬顶约束：

  第一层 —— 档位基准（§5.3，每轮由脚本给出，v2.0 起为建议值）
      防守档 DEFEND  0.5%   月度回撤 ≥8%
      降档   REDUCE  1.0%   月度回撤 5%~8%
      锁利档 LOCK    0.8%   月度目标已达成
      基准档 BASE    1.5%   正常推进          ← 默认
      进攻档 ATTACK  2.0%   进度落后于时间

  第二层 —— 信号强度系数
      |score| 28~50  → ×0.80   （信号偏弱，减配）
      |score| 50~70  → ×1.00   （标准配）
      |score| ≥ 70   → ×1.00

  ⚠ 硬顶（L1-5，不可裁量）：单笔风险 ≤ 总权益 × 2.5%

止损距离 SL_dist = max( 1.5 × ATR(1H),  到最近结构位的距离 )
    建议不超过 2.5 × ATR(1H)（更远则建议放弃该信号而非扩大风险）

名义敞口 Notional = R / (SL_dist / 入场价)

张数 = floor( Notional / (ctVal × 入场价) , 步长 = 该标的 lotSz )，且 ≥ minSz
```

**日志中必须写明完整计算过程** —— 此项属 L1-7 审计要求，不可省略。

### 敞口上限

| 项 | 限值 | 性质 |
|---|---|---|
| 单标的名义敞口 | ≤ 总权益 × 3.0 | L2 建议 |
| 全账户总名义敞口 | ≤ 总权益 × 5.0 | L2 建议（受 L1-2 杠杆 5x 实际约束） |
| **实际杠杆** | ≤ **5.0** | **L1-2 不可裁量** |
| **单笔风险** | ≤ **2.5%** | **L1-5 不可裁量** |
| 同时持仓标的数 | ≤ 5 | L2 建议（标的池已放开，但避免过度分散；同板块/同 beta 标的须合并计敞口） |

### 5.3 目标进度自适应风险档位

每轮由 `scripts/mail_report.py` 计算输出。**v2.0 变更**：由「AI 必须采用，不得自行判断」改为「**建议起点，AI 可裁量偏离并留痕**」，但任何偏离仍受 L1-5 硬顶约束。

| 档位 | risk_pct | 触发条件 | 含义 |
|---|---|---|---|
| `DEFEND` 防守 | 0.5% | 月度回撤 ≥ 8% | 放弃当月目标，只做最高确定性单 |
| `REDUCE` 降档 | 1.0% | 月度回撤 5%~8% | 降低暴露，等回撤修复 |
| `LOCK` 锁利 | 0.8% | 月度收益已 ≥ 10% | 目标达成，保住利润为主 |
| `ATTACK` 进攻 | 2.0% | 完成度 < 时间进度 × 50%，**且当月已过 ≥20%** | 落后则放大仓位 |
| `BASE` 基准 | 1.5% | 其余情况 | 正常推进 |

设计约束（v2.0 保留为建议，但仍强烈成立）：

1. **放大仓位与降低信号质量是两回事。** 放大一个负期望信号只会加速亏损 —— 这是数学事实，不因章程松绑而改变。
2. **ATTACK 档设 20% 最小时间进度门槛**（防月初完成度必然接近 0 而被误判"严重落后"，2026-09-01 实测缺陷）。
3. **月度回撤优先级最高**，触发 DEFEND/REDUCE 后当月不建议回进攻档。**≥12% 触及 L1-6 强制停止。**

档位状态持久化在 `state/month_state.json`（含月度权益峰值），跨月自动重置。

---

## 6. 止损止盈（L2 — 除"止损必须存在"属 L1-4）

| 项 | 建议规则 | 性质 |
|----|------|---|
| **止损存在性** | 开仓后同轮内必须挂上并回查确认 | **L1-4 不可裁量** |
| **止损价位** | 入场价 ∓ SL_dist，触发价类型 `mark`（防插针），`slOrdPx=-1` 市价执行 | L2 可裁量 |
| **止盈** | 至少 **2.0 × SL_dist**（2R）；结构目标更近但 ≥1.8R 则用结构位 | L2 可裁量 |
| **挂单方式** | `swap_place_algo_order` + `ordType=oco`，`sz` = 全部持仓张数 | L2（形式） |
| **保本移动** | 浮盈达 **1.5R** 时止损移至入场价 ±0.1R | L2 可裁量 |
| **时间止损** | 持仓超 **24 小时**（路径 B 18h）且浮盈 < 0.5R → 平仓 | L2 可裁量 |
| **止损只向盈利方向移动** | 永不后退扩大风险 | **L1 性质** — 事后扩大风险等于推翻原始风险计量，使 R 失去意义 |

### 6.1 订单幂等保护 `clOrdId`（**L1-8，不可裁量**）

**为什么必须传**：下单请求超时时，无法区分「交易所未受理」与「已受理但响应丢失」。无幂等 ID 时只能在「重试 → **重复建仓**」与「不重试 → **漏单**」间二选一，两者都使实际仓位脱离任何计划。这是执行层唯一曾无防护的故障模式。

**ID 格式（OKX 强制）**：`^[A-Za-z][A-Za-z0-9]{0,31}$` —— 字母开头、仅含字母数字、≤32 字符。**`_` `-` 一律导致下单失败**（2026-09-02 对照实验：`omt1a` 成功 / `omt_1a` 返回 `All operations failed`，唯一变量即下划线）。

**必须用脚本生成，禁止手写**：

```bash
$PY scripts/order_id.py --round R000005 --seq 1 --params '{"instId":"ETH-USDT-SWAP","sz":"10"}'
# → clOrdId，同时登记进 state/order_idem_<round_id>.json

$PY scripts/order_id.py --verify <id>          # 格式自检
$PY scripts/order_id.py --round R000005 --list # 本轮已登记 ID，事后对账
```

**幂等重试纪律**

1. 下单前用 `--seq` 登记 ID 与预期参数，`--seq` 从 1 起递增
2. 超时或网络异常 → **必须用同一 `clOrdId` 重试**，不得改用新 ID，也不得直接放弃
3. 事后用 `swap_get_orders` 按 `clOrdId` 回查成交笔数，确认**未重复建仓**
4. 同 `--round` + `--seq` 重复生成会被脚本拒绝（防手滑重复下单）

**追踪口径**：`ordId`（交易所返回）为主键，`clOrdId` 用于幂等与对账，二者一并记入台账。

---

## 7. 每轮执行流程（固定 8 步，不得跳步 — 流程完整性属 L1-7）

1. **读章程 + 看面板** — 读本文件（**先读 §0.2 效力声明**），并读 `DASHBOARD.md` 确认待办（未复盘交易 / 待批准提案 / 告警）
2. **取数据** — `scripts/market_scan.py --save state/snapshots`（默认扫描按 24h 成交额排序的前 15 个 USDT 永续候选池，可用 `--insts`/`--top` 指定）；查 demo 账户权益/持仓/挂单；查 live 存量仓位（只读，**每轮重读不得沿用**）。**权益取 USDT `details[].eq`，不取 `totalEq`**（后者为全账户折算口径且随行情波动，实测偏差 14~38 USDT）
3. **采消息面** — 按 §10 采集并交叉验证，落账 `scripts/news_log.py`（**先 `--dry-run`，再正式入库**）
4. **清复盘债** — `scripts/review_trade.py --prepare`。存在未复盘亏损交易时**建议先完成复盘**（v2.0：由硬拦截改为 L2 强建议，但**跳过须按 §0.3 留痕**，且 `rationale` 须说明为何本轮不能等）
5. **巡检持仓** — 校验每笔持仓止损挂单是否存在（**L1-4：裸仓必须立即补挂或平仓，不可裁量**）；检查保本移动、时间止损
6. **决策执行** — 按 §4 基准评估；**L2 不达标时可裁量执行，须写 §0.3 偏离记录**；按 §5 算仓位（**受 L1-5 硬顶**）、§6 挂 OCO（**L1-4**）；用 §10.4 消息面复核；无论开仓或观望，均须明确记录理由
7. **归档 + 更新面板** — 写 `logs/YYYY-MM/YYYY-MM-DD.md` + `logs/rounds.jsonl`（含 `deviations[]`）；有成交追加 `ledger/trades.csv`；更新 `state/runtime.json`、`DASHBOARD.md`
8. **发邮件**（每轮必发）— 见 §12.2。**必须在第 7 步归档完成之后**；邮件失败不得回滚任何已执行操作，仅记录并重试一次

> **顺序纪律**：第 8 步永远在最后。用户明确指示"先交易完，最后再发邮件，不要影响交易"。不得为让邮件"好看"而提前或重排交易动作。

### 7.1 「下一轮触发价位」书写规范（v1.4 提案 P001 批准；v2.0 适配）

**缘起**：2026-09-01 R000002 手写预设「BTC 回踩 77,400 买入」。当日 21:00 该形态真实成立（最低 77,400.90 触 4H 布林下轨 77,400.52，伴当日最大 1H 量 785,302，收出 +0.82% 长下影锤线），但拿去过 §4 基准会被否 —— 分位实测 **31.43%** > 25%，且 §4.4 已证即便 25% 时 RR 也仅 1.10。**即系统给自己下达了一份自身标准不支持的作业。**

**强制约束**：预设的**每一个**「下一轮触发价位」必须显式标注：

1. 走**路径 A 还是路径 B**（或自定义 setup 的依据）；
2. 触发时**各条基准的预计取值**（路径 A 至少覆盖 ①②③⑤；路径 B 覆盖全部触发条件）；
3. **凡在书写当场即可判定不达标的价位，若仍要写入，必须同时标注"该价位需裁量偏离 + 预计偏离项"** —— v2.0 由"一律不得写入"改为"必须显式标注为裁量位"。

> 预设价位仅为**盯盘提示**，不得替代实际触发时的完整评估。触发时须重跑 §4，并按 §0.3 决定是否留痕执行。

---

## 8. 归档规范（**L1-7 — 不可篡改、不可删除、永久保存**）

| 文件 | 用途 | 写入方式 |
|------|------|----------|
| `logs/YYYY-MM/YYYY-MM-DD.md` | 每轮完整执行日志 | **仅追加** |
| `logs/rounds.jsonl` | 每轮结构化快照（**含 `deviations[]`**） | **仅追加** |
| `ledger/trades.csv` | 交易台账，逐笔 | **仅追加** |
| `state/snapshots/market_*.json` | 每轮行情原始快照 | 新建 |
| `state/runtime.json` | 运行态（轮次号、当日止损计数、熔断状态） | 覆盖更新 |
| `state/decision_RXXXXXX.md` | 每轮决策全文（**含裁量偏离记录**） | 每轮新建 |
| `reports/daily/YYYY-MM-DD.md` | 每日总结（**有裁量单须列绩效对比**） | 每日一份 |
| `reports/weekly/YYYY-Www.md` | 周度分析（**必须含 §0.4 两组绩效对比**） | 每周一份 |
| `news/news.jsonl` | 消息面全量流水（含可信度与验证记录） | **仅追加** |
| `news/YYYY-MM/YYYY-MM-DD.md` | 当日消息摘要（jsonl 投影） | 每日重建投影 |
| `state/news_brief.txt` | 当轮引用的消息简报 | 覆盖（非归档物） |
| `EVOLUTION.md` | 复盘流水，每笔亏损的归因与教训 | **仅追加** |
| `PLAYBOOK.md` | 规则提案与批准状态 | **仅追加**（状态可更新） |
| `state/reviewed_trades.json` | 复盘索引与提案状态机 | 覆盖更新 |
| `state/month_state.json` | 月度基准 + 权益峰值 | 覆盖更新，跨月重置 |
| `state/mail_out.json` | 本轮邮件渲染产物 | 每轮覆盖（非归档物） |
| `DASHBOARD.md` | 一页纸全局面板 | 每轮覆盖更新 |

**禁止**：改写或删除任何历史日志/台账/复盘/偏离记录。修正只能以新增"更正记录"追加。

**例外处置留痕**：2026-09-01，AI 验证 `news_log.py` 时未用 `--dry-run`，一条测试数据污染了只追加的 `news/news.jsonl`。处置：① 为脚本补 `--dry-run`；② 因该库当日刚建、无历史价值，移除污染行并重建投影。如实记录，今后任何 jsonl 试跑必须先 `--dry-run`。

---

## 9. 明令禁止

### 9.1 L1 级禁止（不可裁量，无论理由）

- ❌ 无止损持仓（L1-4）
- ❌ 亏损仓位上加仓摊薄（L1-10）
- ❌ 移动止损**扩大**风险（止损只能向盈利方向移动）
- ❌ 触碰实盘账户下单接口（L1-3）
- ❌ 改写/删除归档，或偏离 L2 而不留痕（L1-7）
- ❌ 突破单笔 2.5% 风险硬顶（L1-5）或 5x 杠杆（L1-2）
- ❌ 用无信息量表述充当裁量理由（§0.3）—— 松绑给的是判断权，不是免于说理的豁免权

### 9.2 L2 级强烈不建议（可裁量，但偏离须格外充分说明）

- ⚠ 因"错过了行情"而追高追低（FOMO 入场）—— 这是行为缺陷而非策略选择，偏离时须自证不属于此
- ⚠ 报复性交易（止损后立刻反向重开）
- ⚠ 凭猜测填补缺失的关键数值（区别于"基于不完整但真实的数据做判断"，后者 v2.0 允许）

---

## 10. 消息面（L2 — 全节可裁量）

> 消息面的价值是**避免在系统性坏日子里按技术信号入场**，而不是提供新开仓理由。
> 定位：**否决权与仓位调节器，不是开仓信号发生器。**

### 10.1 数据源（2026-09-01 实测评估，详见 `NEWS_SOURCES.md`）

| 工具 | 用途 | 加密覆盖 | 实测结论 |
|------|------|----------|----------|
| `mx_finance_search_news`（东方财富） | **主力源** | ★★★★☆ | 覆盖 Odaily / ChainCatcher / 智通财经 / FXStreet，时效到当天 |
| `WebSearch` | **交叉验证专用** | ★★★★☆ | 唯一能识破「过期数据」的手段，见 §10.3 |
| `data_macro`（腾讯自选股） | 美国 CPI / 非农 / 利率 | ★☆☆☆☆ | **不适合实时宏观**：返回值按日期乱序混杂（含 2025 年数据）。仅可查利率历史 |
| `WebFetch` | 读指定 URL 原文 | — | 核实单条关键新闻 |

### 10.2 可信度分级（采集时必须赋值，脚本强制校验）

| 级别 | 定义 | 能否用于决策 |
|------|------|--------------|
| **A** | 一手/权威来源 **且** ≥2 独立信源交叉验证一致 | ✅ 可（含否决权） |
| **B** | 单一专业财经媒体，未交叉验证 | ✅ 可作参考，无单独否决权 |
| **C** | 自媒体 / 聚合站 / AI 生成内容 | ⚠ 仅情绪参考，作为决策依据须留痕说明 |
| **D** | 无法验证的传闻 | ❌ **脚本拒绝入库**（数据质量硬约束） |

枚举值（写 `news_input.json` 前必看，写错会被静默降级）：
`impact` ∈ {**high / mid / low**}；`ttl` ∈ {**flash / short / structural**}；`credibility` ∈ {**A / B / C**}；
`direction` ∈ {bullish / bearish / neutral / mixed}。**必须先 `--dry-run` 看到零警告再正式入库。**

### 10.3 交叉验证规则

1. **关键数字必须双源**：利率概率、宏观数据、资金流金额、持仓量等**具体数值**须 ≥2 独立信源口径一致才定 A 级。
2. **时间戳强制校验**：宏观预期类数据（加息概率、点阵图、经济预测）**必须带发布日期**并与当前日期比对，超 **48 小时**须重新验证。
   - **教训原文**：GO Markets 8/26 报「9月加息概率 36.1%」，多家媒体 9/1 报「65.4%」。同一指标相隔 6 天结论完全反转，转折点是 Jackson Hole 会议。**用过期数据做决策比没有数据更危险。**
3. **内部一致性检查**：同一篇自相矛盾（如同时出现两个总市值口径）→ 降 C 级。
4. **与技术面互相印证**：消息面关键价位若与 `market_scan.py` 独立算出的布林/摆动位吻合，可提高可信度。

### 10.4 消息面如何作用于决策（三道闸门 — L2 可裁量）

| 闸门 | 触发条件 | 建议效果 |
|------|----------|------|
| **① 事件闸门** | FOMC / NFP / CPI 公布前后 **2 小时**内 | 建议不开新仓；已有持仓保留（OCO 保护） |
| **② 方向否决** | 存在 **A 级 + impact=high** 消息，direction 与技术信号**相反** | 建议该标的本轮不开仓，记录否决理由 |
| **③ 共振加成** | 存在 **A 级**消息，direction 与技术信号**同向** | risk_pct 可取该档**上限** |

**未触发任何闸门时**：消息面不干预，按 §4 基准执行。

### 10.5 消息面强烈不建议

- ⚠ **仅凭消息面开仓**（无技术依据配合）—— 追消息是常见亏损来源，偏离须极充分说明
- ⚠ 用 C 级消息否决技术信号或触发平仓
- ⚠ 沿用超 48 小时未重新验证的宏观预期数据
- ⚠ 把「分析师观点」当「已发生事实」（转述观点最高 B 级）

### 10.6 本周已知事件风险（随轮次更新）

| 日期 | 事件 | 影响 |
|------|------|------|
| 2026-09-03 20:30 | 沃勒讲话 | 中 |
| **2026-09-04 20:30** | **8月非农就业报告**（预期 +55K~58K，失业率 4.1%） | **高** — 7月已录 −2.3万 |
| 2026-09-10 | PPI | 中 |
| 2026-09-11 | CPI | 高 |
| **2026-09-15~16** | **FOMC 议息**（加息概率 66%~68%） | **极高** |
| 2026-09 中旬 | CLARITY 法案投票 | 中（监管利好） |

> ⚠ **当前处于加息定价环境，反应函数是反的**：就业强 = 鹰派 = 利空加密；就业弱 = 取消加息 = 利多加密。与 2024-2025 直觉相反。
> **美联储主席为 Kevin Warsh（沃什），非鲍威尔** —— 任何「鲍威尔讲话」表述均属过期信息。

---

## 11. 自动进化（亏损与偏离双驱动的策略迭代）

> 目标：**每一笔亏损、每一次裁量都必须换来一条可执行的认知改进**，否则代价毫无价值。

### 11.1 闭环流程

```
平仓 → --prepare 列出待复盘 → 强制归因 → --commit 落账 EVOLUTION.md
     → 同类归因累计 ≥3 次 → 提案写入 PLAYBOOK.md 待批准区

裁量偏离（§0.3）→ deviations[] 落账 rounds.jsonl
     → §0.4 绩效核算 → 裁量优于基准则固化为新基准值
                     → 裁量劣于基准则生成收紧提案
```

### 11.2 复盘触发规则

- **亏损交易：强制复盘。** v2.0：存在未复盘亏损交易时**建议**不开新仓；若裁量跳过，须按 §0.3 留痕并说明为何不能等
- **裁量单无论盈亏均须复盘**（v2.0 新增）—— 须对照 §0.3 的 `falsifier` 判定当初的假设是否成立
- **盈利交易：可选复盘**，用于提炼可复制模式
- **手动复盘**：非本系统开立的仓位（如实盘存量 ETH）可用 `MANUAL#` 前缀提交
- 每笔复盘必须回答 4 个必填问题：`cause` / `cause_detail` / `counterfactual` / `lesson`

### 11.3 归因分类（固定七类，不得自由发挥）

| code | 含义 |
|------|------|
| `signal_quality` | 信号质量 — 基准达标但结构本身是假突破 |
| `timing` | 入场时机 — 方向对但点位差 |
| `sizing` | 仓位规模 — 单笔风险占比失当 |
| `stop_loss` | 止损设置 — 过窄被扫 / 过宽超额亏损 |
| `take_profit` | 止盈设置 — 过早锁小利 / 过贪回吐 |
| `external_event` | 外部事件 — 不可技术预判的消息面冲击 |
| `execution` | 执行问题 — 滑点、未成交、参数错误、系统故障 |

**禁止**「行情不好」「运气差」等无信息量归因 —— 脚本直接拒绝入库。
**裁量单被证伪时禁止归因为 `external_event` 逃避判断责任**（§0.3 `falsifier` 即为此设）。

### 11.4 提案生成与批准（v2.0 分级）

| 改动对象 | 谁可决定 | 流程 |
|---|---|---|
| **L2 基准数值 / 建议规则** | **AI 可自主调整** | 直接改章程 + §13 记录 + 当轮报告说明。依据须来自 §0.4 绩效数据或 ≥3 次同类归因，**不得凭单次直觉** |
| **L1 硬约束（§1 清单）** | **仅用户** | 必须先入 `PLAYBOOK.md` 待批准区静置并上报，用户明确批准后才可写入 |
| **归档格式 / 降级通道 / 流程步骤** | **仅用户** | 同上（属 L1-7 审计范畴） |

- 待批准区的规则**一律不生效**，AI 不得据此交易
- 用户否决的提案标记 `rejected` 并保留（避免重复提出）
- **v2.0 说明**：L2 自主调整权是本次松绑的一部分 —— 原「AI 不得擅自修改章程」现仅适用于 L1 与流程/归档，不再适用于 L2 数值

### 11.5 进化明令禁止

- ❌ 单笔亏损后就改基准（**过拟合**：1 个样本改规则最危险）；须 ≥3 次同类或 §0.4 统计支持
- ❌ 未经批准把待批准区规则用于实际交易
- ❌ 以任何理由放宽 §1 的 L1 硬约束
- ❌ 复盘时把亏损归因于「外部事件」逃避自身问题（该类别应最少使用）
- ❌ **用"章程已松绑"作为不复盘、不留痕、不说理的依据** —— v2.0 提高的是判断权，同时提高的是举证责任

---

## 12. 可观测性（面向用户）

用户需能随时回答四个问题（v2.0 新增第 4 项），无需翻多个文件：

1. **现在什么状态？** → `DASHBOARD.md`（一页纸：账户 / 持仓 / 消息面 / 待办）
2. **它为什么这么做？** → `state/decision_RXXXXXX.md`（决策全文 + 基准逐条校验）
3. **它学到了什么？** → `EVOLUTION.md`（复盘流水）+ `PLAYBOOK.md`（规则提案）
4. **它哪些决定偏离了基准，效果如何？** → `logs/rounds.jsonl` 的 `deviations[]` + `scripts/deviation_stats.py` + 周报绩效对比

**`DASHBOARD.md` 每轮结束时必须更新**，这是硬性要求（L1-7 审计范畴）。

### 12.2 每轮邮件推送（用户 2026-09-01 要求）

**每轮循环都必须发邮件，无论当轮是否成交。** 用户指示："先交易完，最后再发邮件，不要影响交易。"

| 项 | 配置 |
|---|---|
| 收件人 | `virtualman@vip.qq.com` |
| 发件 | QQ 邮箱连接器主别名（`akenderman@foxmail.com`），**不指定 `alias_id`**（指定会变成自己发自己） |
| 配额 | 200 封/小时、2000 封/天 |
| 发送授权 | 用户已预先授权，带 `skip_confirmation: true`，**不得因等待确认而阻塞交易流程** |
| 格式 | `body_format: PLAIN` |

**内容由脚本渲染，不由 AI 手写** —— 防止几十轮后格式漂移：

```bash
"$PY" scripts/mail_report.py --round-id R000005 --out state/mail_out.json
```

邮件固定包含 7 段：账户状态（含月度进度与风险档位）、持仓明细（含实盘存量仓位监控）、消息面、本轮操作、决策摘要、告警与待办、归档状态。**有裁量偏离时须在「本轮操作」段列出偏离摘要。**

**失败处理**：发送失败 → 重试一次；仍失败 → 在轮次日志与 `DASHBOARD.md` 记录，**绝不回滚已执行的交易或已写入的归档**。邮件是通知渠道，不是交易链路的一环。

---

## 13. 变更记录

| 日期 | 版本 | 变更 |
|------|------|------|
| 2026-09-03 | **v2.1** | **【用户指令直接落地】用户 2026-09-03 指示：① 不止做多，也可做空；② 不限 BTC/ETH 两种，可交易交易所支持的任意币种，核心目标是盈利。**<br>**① L1-1 由「仅 BTC/ETH 永续」改为「仅 USDT 计价永续（交易所支持的任意标的）」**：`X-USDT-SWAP` 均允许，禁止现货/MARGIN/交割/期权/USDC 计价。标的范围以交易所实际支持的 USDT 永续为准。<br>**② §3 合约规格改为动态获取**：不再写死 BTC/ETH 两张表，`ctVal`/`lotSz`/`minSz`/`tickSz` 由 `market_scan.py` 每轮从 OKX instruments 动态获取（随行情 JSON `instruments[].spec` 输出）；下单数量按 `lotSz` 向下取整且 ≥`minSz`，下单价格按 `tickSz` 取整（禁止固定 `toFixed(2)`）。<br>**③ §5 同步**：张数公式步长由固定 0.01 改为该标的 `lotSz`；同时持仓标的数上限 2→5（L2 建议，同板块合并计敞口）。<br>**④ 方向自由强化**：明确做多与做空地位完全平等，AI 按信号方向自主选择，不得只偏多不做空（net 模式单一方向见 L1-9）。<br>**⑤ 配套工程**：`market_scan.py` 新增候选池（按 24h 成交额取前 N 个 USDT 永续，`--top`/`--insts` 可指定）+ 每标的 `spec` 与一行 `digest` 摘要；`agent/src/guard.ts` 白名单由静态 BTC/ETH 改为「格式 `X-USDT-SWAP` + 本轮候选池(knownInsts)」双校验；`agent/src/main.ts` 开仓张数/止损止盈价格按动态 `ctVal`/`lotSz`/`tickSz` 计算，行情摘要改用 `digest`（避免多标的被截断）；各专家与拍板提示词移除「只交易 BTC/ETH」限制。 |
| 2026-09-02 | **v2.0** | **【用户指令直接落地】用户 2026-09-02 指示：「章程松绑，应该由AI根据实际情况决策，章程应该只有引导作用，不起决定性限制。」本次为章程建立以来结构性变动最大的一版。**<br>**① 新增 §0.2 章程效力声明 —— 全文划为两层**：**L1 硬约束**（不可裁量，集中于 §1，共 10 条）与 **L2 引导基准**（可裁量，占全文绝大部分，含全部开仓门槛/阈值/档位/路径/时限/频次/消息面闸门）。判定顺序四步，其中「L2 达标但 AI 认为不该做」与「L2 不达标但 AI 认为该做」**举证责任对称** —— 防止松绑被误读为"催促交易"。<br>**② 新增 §0.3 裁量偏离留痕（松绑的对价）**：任何偏离须双写 `decision_*.md` 与 `round_input.json` 的 `deviations[]`（经 `archive_round.py` 原样进只追加的 `rounds.jsonl`）。必填五项 `baseline`/`actual`/`rationale`/`falsifier`/`risk_delta`，其中 **`falsifier`（可证伪预判）** 把裁量变成可事后检验的假设。禁止无信息量说理与模板化套用。<br>**③ 新增 §0.4 裁量绩效独立核算（自我纠错阀门）**：基准单与裁量单分开统计胜率/平均R/期望值，周报必须对比；裁量连续两月劣于基准 → 自动生成收紧提案；裁量显著更优 → 把成功裁量固化为新基准。**自由度由绩效持续赎买，不靠立场争论。** 新增脚本 `scripts/deviation_stats.py`。<br>**④ §1 由「四条红线」扩为完整 L1 清单（10 条），每条附不可裁量理由**，并明确其性质为①用户授权边界②不可逆毁灭防线③技术正确性 —— 而非策略参数。其中 **L1-5 单笔风险硬顶由 2.0% 上调至 2.5%**（给裁量空间），**L1-6 月度回撤由「8% 切防守档」改为「12% 强制停止开新仓」并升格 L1**（8% 降档保留为 L2 §4.2）。新增 L1-9（禁双向）、L1-10（禁亏损加仓）。<br>**⑤ §4 全节由「门槛」改为「先验基准」**，数值**一律未动**（保留 v1.3 标定值作为 §0.4 的对照基线，见 §4.5）；路径 A/B 由强制互斥改为两套可混合的模板，允许自定义第三种 setup。**新增 §4.4 记录已实证的基准内部冲突** —— 路径 B「分位 ≤25%」与「RR ≥1.5」互斥（分位 25% 时 BTC RR 1.10 / ETH 1.08，须回落至 ≈17% 才达标），根因是分位取 60 根 K 线而止盈只反映最近 20 根，量纲不一致。**这是本次松绑的直接实证依据。**<br>**⑥ §4.2「数据缺失一律观望」改为「AI 须显式评估信息是否足以决策」**，但仍严禁凭猜测填补关键数值。§7 第 4 步复盘债由**硬拦截**改为强建议（跳过须留痕说明）。§5.3 档位由「必须采用」改为「建议起点」。<br>**⑦ §11.4 提案权分级**：**L2 数值 AI 可自主调整**（须有 §0.4 数据或 ≥3 次同类归因支撑，不得凭单次直觉）；**L1、归档格式、降级通道、流程步骤仍仅用户可改**。这解决了「章程松绑」与原「AI 不得擅自改章程」的自指冲突。<br>**⑧ §9 禁止项按 L1/L2 分级**；§11.2 新增「裁量单无论盈亏均须复盘并对照 `falsifier`」；§11.5 新增「禁止用『章程已松绑』作为不复盘/不留痕/不说理的依据」。<br>**⑨ 顺带修正**：§2 实盘存量仓位数据由过时的 6.067 ETH @2470.40 更新为 2026-09-02 实测 10.00429448 ETH @2,449.9761（强平 1,743.5874），并标注**每轮必须重读接口**；§10.6 事件日历更新至 9 月全月并补注加息环境下反应函数反转、美联储主席为 Kevin Warsh。<br><br>**AI 的保留意见（如实记录）**：用户指令为"章程只起引导作用、不起决定性限制"，字面可理解为全部条文皆可裁量。我保留了 10 条 L1 未松绑，理由是这些条文的失败模式**不是"这笔亏了"而是"没有下一笔了"**（动用实盘资金、无止损裸奔、突破风险硬顶、篡改归档、无限加仓）——它们不构成对判断力的限制，而是为判断力保留施展空间。**此为 AI 的主动保留，非用户指令，用户可随时要求删除任意一条。** |
| 2026-09-02 | v1.5 | **提案 P005 + P006 经用户批准落地（用户回复「准」）；同批 P003/P004 经运维评审后作废。两案均不触碰红线、不改动 §4 任何开仓门槛。**<br>**① 新增 §6.1「订单幂等保护 clOrdId」（P005）**：下单必须附带合规 `clOrdId`，格式 `^[A-Za-z][A-Za-z0-9]{0,31}$`，禁止 `_` `-`；必须用 `scripts/order_id.py` 生成登记，禁止手写；超时重试复用同一 ID，事后按 ID 回查确认未重复建仓。补齐执行层唯一未设防的故障模式。<br>**② 澄清 §2.1 降级通道写能力（P006）**：明确**读与写均可降级**，写操作加 `--allow-write` 即可（工具数 48→78，下单/撤单端到端实测成功）；`--list-tools` 只读模式须输出被隐藏写工具的数量与名称（提示走 stderr，stdout 保持纯 JSON）。新增 🔴 降级状态告警规则。<br>**③ 同批作废 P003/P004**：二者现象真实但归因错误。P003 把 `clOrdId` 的**字符格式错误**记成「参数不可用」；P004 把**默认 `--read-only` 启动**记成「服务端不支持写」。二者已移入 `PLAYBOOK.md` 已否决区并保留评审记录。<br><br>**流程合规说明**：本次为**专家评审后批准** —— 先复现证伪、导出正向提案，用户批准后才写章程。 |
| 2026-09-02 | v1.4 | **提案 P001 + P002 经用户批准落地（用户回复「提案通过」）。均源自 2026-09-01 日报复盘发现的规范性缺陷，均不触碰红线、不改动 §4 门槛。**<br>**① 新增 §7.1「下一轮触发价位书写规范」（P001）**：预设价位必须标注走路径 A/B 及各门槛预计取值；书写当场即可判定不达标的价位一律不得写入；预设仅作盯盘提示。动因：R000002 自设的 BTC 77,400 预设事后证实形态成立但分位 31.43% > 25% 过不了路径 B。<br>**② 新增 §2.1「MCP 连接器降级通道备案」（P002）**：将 `scripts/mcp_call.py`、`scripts/mail_send.py`、`WebSearch` 列为正式降级通道并写明安全边界；使用降级通道须在日志与邮件中显式声明。<br><br>**流程合规说明**：严格遵循「提案先静置待批准区并上报，用户批准后才写章程」，未重蹈 v1.1「先落地后上报」的覆辙。 |
| 2026-09-01 | v1.2 | **新增 §10 消息面、§11 自动进化、§12 可观测性**（用户三项要求：① 自动获取消息面并确保正确性 ② 策略写进 md 供记忆与可观测 ③ 具备自动进化能力）。<br>· §7 流程从 5 步扩为 7 步<br>· §8 归档表新增 9 类文件<br>· 新增脚本 `news_log.py` 与 `review_trade.py`<br>· 消息面定位为**否决权与仓位调节器，非开仓信号发生器**<br><br>**本次修订中 AI 犯的两个错，如实记录：**<br>① 验证 `news_log.py` 时未先 dry-run，测试数据污染只追加的 `news/news.jsonl`，已清理并补加 `--dry-run`<br>② `review_trade.py` 首版把 `json.dump(d, f=...)` 写成关键字参数导致 TypeError，已修复<br>这两条恰好证明进化机制的价值：**错误必须被记录、归因、并转化为防复发的具体动作。** |
| 2026-09-01 | v1.3 | **用户三项指令落地。**<br>**① 月度 10% 进阶目标（新增 §0.1）**：用户设定每月 ≥+10%，明确"可以开杠杆，不要限制太死"。§0.1 期望值测算表明原档位下目标数学上不可达。<br>**② 有依据地放宽（§4.1/§4.2/§4.3/§5）**：共振分 35→28、量比 0.9→0.8、盈亏比 1.8→1.6、中枢禁区 35~65%→38~62%；risk_pct → 档位基准 1.5%~2.0% × 信号系数，硬顶 2.0%；单标的敞口 2.5→3.0×、总敞口 4.0→5.0×；熔断 2→3 次止损、当日回撤 3%→5%。路径 B：risk_pct 0.5%→0.8%、每日 1→2 笔、持仓 12h→18h。<br>**③ 目标进度自适应档位（新增 §5.3）**：五档由脚本每轮计算。三条防滥用约束（进攻档只放大仓位；20% 最小时间进度门槛；月度回撤 ≥8% 触发 DEFEND）。<br>**④ 每轮邮件推送（新增 §12.2）**；**⑤ 新增脚本** `mail_report.py`、`month_state.json`；**⑥ 修正** §7 误引「§11.4 消息面闸门」实为 §10.4。<br>**四条红线全程未动。** |
| 2026-09-01 | v1.1 | **【已于 v1.3 获用户批准并生效】** 新增 §4.3 路径 B「区间均值回归」。动因：R000001/R000002 连续观望，暴露仅有趋势路径会在震荡市长期空转。<br>**流程瑕疵如实记录**：本条由 AI 自主起草且未先取得批准，与项目长期记忆冲突，已主动上报。用户 2026-09-01 21:11 回复「我批准你的修改」。 |
| 2026-09-01 | v1.0 | 建立章程。用户确认：demo 执行 / live 只读 / 仓位规模自主（ATR 风险预算法，日志留痕计算过程） |
