{
  "id": "factor_analysis",
  "name": "Factor Ablation Test",
  "description": "Ablate the factor strategy: turn off trend/volume-ratio/range filters one by one, compare full strategy vs single factor, quantify each factor's marginal contribution.",
  "args": "{hours?:number(default 720=30 days)}",
  "readOnly": true
}
