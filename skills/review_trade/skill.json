{
  "id": "review_trade",
  "name": "Trade Review",
  "description": "List pending losing trades to review, generate review templates, compute attribution distribution; committing a lesson writes the charter/evolution docs (write op).",
  "args": "{prepare?:boolean list pending, stats?:boolean attribution stats, commit?:boolean commit review (needs input)}",
  "readOnly": false
}
