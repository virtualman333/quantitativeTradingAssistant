{
  "id": "news_query",
  "name": "Query Trusted News DB",
  "description": "Query the SQLite news DB for trusted news from the last N hours (A=dual-source verified / B=single professional source); no re-fetch or re-verify needed. Prefer this to reuse trusted history.",
  "args": "{hours?:number(default 24), minCred?:'A'|'B'|'C'(default B), limit?:number(default 40)}",
  "readOnly": true
}
